# legacy/ — vorübergehend

Bausteine aus `ludwig/app`, auf denen v3 aufsetzt, aber noch nicht in die
Dreiteilung `primitives/patterns/entities` eingeordnet sind: Status-Registry
und StatusBadge, Banner, LongText, Pagination, Buchungs-Formatierung.

**Nichts Neues hier hinein.** Jeder Baustein wandert nach `src/ui/v3/`, sobald
er dort gebraucht und abgenommen ist; danach verschwindet dieser Ordner.
Exportiert wird weiterhin nur über `src/ui/v3/index.ts`.
