# 0033 · Time — ein Zeitpunkt, fünf Formate, plus Dauer

| | |
|---|---|
| Status | fertig |
| Stufe | `primitives/` |
| Klassen-Test | „Ergäbe das auch in einer Versicherungs-App Sinn?" → ja, ein Zeitpunkt ist fachfrei |
| Quelle | `ludwig/app` `docs/topics/web-ui-offen.md` **P24**, Ziel des Owners vom 2026-09-03 |
| Ersetzt | 9 Datums-Formatierer (`formatGermanDate(Time)`, `fmtDate` ×2, `fmtDateTime`, `fmtDateShort`, `fmtDateFull`, `fmtTimestamp`, `formatCutoffDate`) und **83 `toLocale*`-Stellen in 64 Dateien**; dazu `TimeAgo` (v1) |
| Blockiert | P3 geht hier auf — die 83 Stellen rechnen ohne `timeZone` und liegen zwei Stunden daneben |
| Spec von / am | Claude, 2026-09-03 |

## Ziel

„Wann kam der Beleg?" beantwortet Ludwig in neun Formaten, und 83 Stellen
rechnen dabei ohne Zeitzone — die zeigen im Zweifel den Vortag. `Timestamp`
kann genau ein Format (`dd.MM. HH:mm`) und kein Datum ohne Uhrzeit; eine
Dauer („läuft seit 14 Min.") gibt es gar nicht, sie steht als Text im Code.

## Einordnung

- **Wiederverwenden:** `Timestamp` ist dieses Format, eines von fünfen. Es
  bleibt als Zell-Variante und setzt auf diesen Baustein auf.
- **Neu, weil:** Regel 3 — kein `@when` deckt Datum, Monat, relative Zeit und
  Dauer ab; kein Fachwort; 64 belegte Dateien.
- **Zuschnitt:** Familie in einer Datei — `Time` (ein Zeitpunkt) und
  `Duration` (eine Zeitspanne). Eine Dauer ist keine Uhrzeit: sie kommt als
  Sekunden, nicht als ISO-Wert; ein gemeinsamer Prop-Typ wäre eine Union, die
  an jeder Aufrufstelle erklärt werden müsste. Die Funktionen liegen in
  `src/ui/v3/format.ts` — dieselbe Datei wie bei 0032, ein Modul für beides.
- **Setzt auf:** `IsoDate`/`IsoDateTime` aus `src/ludwig/shared/dates.ts`.

## Schnittstelle

`Time`:

| Prop | Typ | Pflicht | Bedeutung | Nachweis (Story) |
|---|---|---|---|---|
| `value` | `IsoDate \| IsoDateTime \| Date \| null` | ja | Der Zeitpunkt; `null` ist der Gedankenstrich | `Empty` |
| `format` | `"date" \| "dateTime" \| "relative" \| "month"` | nein | Default `dateTime` | `Formats` |
| `length` | `"short" \| "medium" \| "long"` | nein | Nur bei `dateTime`: `26.08. 09:12` · `26.08.2026 09:12` · `Mittwoch, 26. August 2026 um 09:12` | `Lengths` |
| `size` | `"sm" \| "md"` | nein | wie bei `Amount` | `Sizes` |
| `prefix` | `string` | nein | „zuletzt", „seit" — ein Wort davor | `InUse` |

`Duration`:

| Prop | Typ | Pflicht | Bedeutung | Nachweis (Story) |
|---|---|---|---|---|
| `seconds` | `number \| null` | ja | Die Spanne | `Durations` |
| `size` | `"sm" \| "md"` | nein | | `Durations` |

`format.ts` liefert `formatTime(value, format, length)` und
`formatDuration(seconds)` — dieselben Regeln ohne React.

**Kann bewusst nicht:** Zeitzonen wählen (es ist immer Europe/Berlin, R3),
Zeiträume darstellen (das ist `DateRangeField`, 0024), rechnen.

## Verhalten

- **Immer `Europe/Berlin`** — das ist der Kern der Aufgabe. Ein Zeitpunkt ohne
  Zeitzone ist der Fehler, den P3 in 83 Stellen zählt.
- **Ein kalendarisches Datum verschiebt sich nicht.** `2026-08-26` ist ein
  Tag, kein Zeitpunkt: es wird aus seinen Teilen gesetzt, nicht über
  `new Date(...)` mit Zeitzonen-Umrechnung. Sonst wird aus dem Belegdatum je
  nach Ort der Vortag.
- **`relative`** zeigt „vor 3 Tagen" und trägt die absolute Zeit im `title`.
  T7 verlangt die absolute Angabe **neben** der relativen; der Owner hat in
  P24 den `title` dafür bestimmt. Wer die Zeit ohne Zeigen braucht, nimmt
  `dateTime` — siehe offene Frage 1.
- **`month`** ist für Achsen und Gruppenköpfe: `August 2026`.
- **Dauer**: unter 60 s mit einer Nachkommastelle (`4,2 s`), darunter keine
  Millisekunden; bis 60 min ganze Minuten (`14 Min`); darüber Stunden und
  Minuten (`2 Std 5 Min`). Kein „vor", kein „seit" — das ist `prefix` beim
  Aufrufer.
- Gerendert wird ein `<time dateTime=…>`, außer bei `Duration` — eine Spanne
  hat kein Datum.
- Server-Components. `relative` rechnet beim Rendern, nicht tickend: ein
  Zähler, der im Sekundentakt springt, zieht das Auge von der Arbeit weg (V12).

## Stories

Titel `v3/Primitives/Werte/Time`. Abgeleitet nach §6: 2 Zustände (gefüllt,
leer) + 2 Enums (`format`, `length`) + 0 Callbacks + 1 „im Einsatz"
+ 1 Rand (Jahreswechsel, Sommerzeit) + 1 zweiter Export (`Duration`) = 7.

| Story | Beweist |
|---|---|
| `Formats` | `date`, `dateTime`, `relative`, `month` untereinander |
| `Lengths` | `short`, `medium`, `long` am selben Zeitpunkt |
| `Durations` | 4,2 s · 14 Min · 2 Std 5 Min · 0 s |
| `Sizes` | `sm` und `md`, in einer Zeile fluchtend |
| `Empty` | `null` ist ein Gedankenstrich |
| `Edges` | reines Datum ohne Uhrzeit, Jahreswechsel, Sommer-/Winterzeit |
| `InUse` | in einer Tabellenzeile und in einer `Timeline` |

Nicht anwendbar: `LeerNachFilter`, `Laedt`, `Fehler`.

## Abnahme

Abgenommen am 2026-09-05 von einem zweiten Agenten (nicht dem bauenden), in
zwei Durchgängen: erste Prüfung auf `e3c38e7` → zurück auf `in Arbeit` mit vier
Mängeln; **Nachprüfung auf `a3bc067` → alle vier behoben, Status `fertig`.**
Nachweise vom laufenden Storybook auf Port 6107, gemessen in einer eigenen
Chromium-Instanz (1280 × 900); die Zeitzonen-Proben mit `timezoneId` bzw.
`TZ=…`.

### Story-Deckung

`index.json` listet die sieben abgeleiteten Stories: `--formats`, `--lengths`,
`--durations`, `--sizes`, `--empty`, `--edges`, `--in-use`. Jede Prop hat jetzt
ihren Nachweis:

| Prop / Wert | Nachweis | |
|---|---|---|
| `value` | `Empty` (`null`), `Edges` (reines Datum, unlesbarer Wert) | ✓ |
| `format` | `Formats`, **fünf** Werte: `date`, `dateTime`, `relative`, `age`, `month` | ✓ |
| `length` | `Lengths` — `short`/`medium`/`long` am selben Zeitpunkt | ✓ |
| `size` | `Sizes` — 12.5 px und 13.5 px gemessen, `tabular-nums` beide | ✓ |
| `prefix` | `InUse`, Spalte „Fällig": gerendert „am 14.09.2026" und „seit 02.09.2026" | ✓ |
| `Duration.seconds` / `size` | `Durations`, `Sizes` | ✓ |

`InUse` zeigt beide von der Spec verlangten Orte: die Tabellenzeile **und**
eine `Timeline` darunter, deren Einträge ihre Zeitpunkte von demselben
Baustein bekommen (im Screenshot als Tagesköpfe „Mittwoch, 26. August 2026" mit
Uhrzeiten 09:12 / 14:00). `LeerNachFilter`, `Laedt`, `Fehler` sind oben
begründet ausgeschlossen.

| Kriterium | Nachweis (Story-ID · Befehl · Beobachtung) | Ergebnis |
|---|---|---|
| `pnpm typecheck` und `pnpm build` grün | `pnpm typecheck` → `tsc --noEmit`, Exit 0, in beiden Durchgängen. `pnpm build` bewusst **nicht** gelaufen (parallele Abnahmen schreiben nach `storybook-static`); der Lauf für diesen Stand war grün — „Storybook build completed successfully". | ✓ |
| Datei nach der Familie benannt, Story daneben, Titel in der richtigen Gruppe | `src/ui/v3/primitives/Time.tsx` und `Time.stories.tsx` nebeneinander; `Time.stories.tsx:7` setzt `title: "v3/Primitives/Werte/Time"`. Barrel: Gruppe „Werte" `index.ts:132`, Export `:134`. Die Familie liegt in einer Datei (`Time` + `Duration`), wie der Zuschnitt es sagt. | ✓ |
| Code englisch; `@when`/`@instead` an jedem Export | Zwei Exporte: `Time` (`Time.tsx:20`, `@when` Z. 14–16, `@instead` Z. 17–18) und `Duration` (`:55`, `@when` Z. 52, `@instead` Z. 53). Alles englisch außer Nutzertext. Siehe Anmerkung 1 zu `format.ts`. | ✓ |
| Kein Hex, kein px, keine lokale Label-Map; Status nur über Registry | `grep -nE "#[0-9a-fA-F]{3,8}\b\|[0-9]+px" src/ui/v3/primitives/Time.tsx src/ui/v3/format.ts` → keine Treffer. Maße in `v3.css:2025–2027`. Kein Status, keine Label-Map. | ✓ |
| Alle Stories oben vorhanden; ausgeschlossene Zustände begründet | Sieben von sieben; jede Prop und alle fünf `format`-Werte belegt, siehe „Story-Deckung". **Im ersten Durchgang ✗** (M2–M4), in `a3bc067` behoben. | ✓ |
| Prüfliste `design-guidelines.md` §9 durchgegangen | Punkt für Punkt unter dieser Tabelle. | ✓ |
| Im Browser angesehen (Storybook), nicht nur gebaut | Alle sieben Stories in beiden Durchgängen geöffnet, gemessen und als Screenshot gesehen. Konsole jetzt in **allen** sieben leer — die React-Fehler in `--in-use` sind mit dem Umbau auf `<span>` weg (Anmerkung 2). | ✓ |
| `2026-08-26` als reines Datum zeigt den **26.**, unabhängig von der Rechner-Zeitzone | **Behoben in `a3bc067`.** `toDate` (`format.ts:112–119`) verankert den Kalendertag auf `Date.UTC(y, m-1, d, 12)`. Weil das ein absoluter Zeitpunkt ist, hängt das Ergebnis gar nicht mehr an der Rechner-Zone — nachgemessen an der Story `--edges` in **fünf** Zonen von UTC−8 bis UTC+14 (Europe/Berlin, Asia/Tokyo, Pacific/Auckland, **Pacific/Kiritimati** = UTC+14, America/Los_Angeles): überall `["26.08.2026","01.01.2026","29.03.2026, 04:30","25.10.2026, 03:30","—"]`, Zeichen für Zeichen identisch. Ohne Browser gegengeprüft in sieben Zonen inklusive **Pacific/Midway** (UTC−11) für `2026-08-26`, `2026-01-01`, `2025-12-31`: jedes Mal dasselbe Datum, `formatTimeFull` mitgeprüft. Die Sommer-/Winterzeit-Zeilen sind unverändert richtig (02:30 UTC → 04:30 bzw. 03:30 Berlin). Der Kommentar über `toDate` erklärt jetzt **beide** Fehlerrichtungen — UTC-Mitternacht wie lokale Mitternacht —, damit der Anker nicht in die eine oder andere Ecke zurückgedreht wird. | ✓ |
| Jede Formatierung nennt `timeZone: "Europe/Berlin"` | `format.ts:16` `const TZ = "Europe/Berlin"`, gesetzt in allen fünf `Intl.DateTimeFormat`-Instanzen. `grep -n "toLocale" src/ui/v3/format.ts` → keine Treffer. | ✓ |
| `relative` trägt die absolute Zeit im `title` | Story `--formats`, DOM-Probe: `<time datetime="2026-09-02T12:00:50.088Z" title="Mittwoch, 2. September 2026 um 14:00">vor 3 Tagen</time>`. Der `title` steht an **allen** fünf Formaten, nicht nur an `relative` (`Time.tsx:43`). | ✓ |
| Dauer: 4,2 s · 14 Min · 2 Std 5 Min stimmen auf die Sekunde | Story `--durations`, gelesene Reihe für `[0, 4.2, 42, 59.9, 60, 840, 3600, 7505, null]`: `0,0 s · 4,2 s · 42,0 s · 59,9 s · 1 Min · 14 Min · 1 Std · 2 Std 5 Min · —`. 7505 s = 125,08 min → gerundet 125 → 2 Std 5 Min. (Die Story-Tabelle oben schreibt „0 s"; gerendert wird „0,0 s" — richtig nach der Regel „unter 60 s mit einer Nachkommastelle".) | ✓ |
| Gerendert wird `<time dateTime=…>` | Story `--formats`, alle fünf Elemente sind `TIME` mit gesetztem `datetime`; `Duration` ist bewusst ein `<span>` (`Time.tsx:63`) — eine Spanne hat kein Datum, wie die Spec sagt. Bei unlesbarem Wert bleibt `datetime` leer statt falsch (`--edges`, letzte Zeile). | ✓ |
| `Timestamp` setzt auf `Time` auf, statt ein zweites Mal zu formatieren | `Cells.tsx:2` importiert `formatTime` und `formatTimeFull`, `:89–91` ruft sie („The short form of `Time` (P24) — this cell is its table-shaped variant"). Kein zweites `Intl.DateTimeFormat` in `Cells.tsx`. Der Zeitzonen-Fix wirkt damit auch dort. | ✓ |
| `formatTime`/`formatDuration` sind ohne React aufrufbar | `format.ts` hat genau einen Import, und der ist ein Typ (`:1`). Headless: `npx tsx -e "…"` → `26.08.2026, 09:12 \| 26.08.2026 \| August 2026` und `4,2 s \| 14 Min \| 2 Std 5 Min \| —`, dazu `react loaded? false`. | ✓ |

### Prüfliste §9, Punkt für Punkt

- **Stufe und Importe** — ✓ `primitives/`, die Komponente importiert nur
  `../format`. Die **Story** importiert für `InUse` zusätzlich `Timeline` aus
  `../patterns/` — das ist Hauspraxis für Einsatz-Stories (`Kbd`, `Markdown`,
  `PageHeader` machen es genauso) und betrifft nicht die Importrichtung des
  Bausteins.
- **Ersetzt ihr v1-Gegenstück** — offen (App).
- **Kein Hex, kein px, keine Label-Map** — ✓.
- **Text links, Zahlen rechts mit `tnum`, nichts zentriert (V3)** — ✓ jetzt
  auch in der Story: die Spaltenköpfe sind `<span>` und messen `text-align:
  start`, nur „Betrag" steht auf `right` (`v2num`). Der zentrierte Kopf aus dem
  ersten Durchgang ist weg.
- **Zeilenhöhe ≤ `.v2tbl__row`** — ✓ `.v2time` bringt keine eigene mit.
- **Farbe** — ✓ keine; ein Zeitpunkt trägt keine Kritikalitätsstufe.
- **Fünf Zustände** — ✓ gefüllt und leer gebaut, drei begründet ausgeschlossen.
- **Kontrast** — ✓ erbt die Textfarbe; `null` in `--color-text-muted`.
- **Tastatur / Hover** — nicht anwendbar. **Anmerkung 4:** die absolute Zeit
  hängt allein am `title` und ist damit nur per Maus erreichbar; die offene
  Frage 1 der Spec vermerkt den Konflikt mit T7 ausdrücklich. Kein Mangel
  gegen die Spec, aber der Punkt lebt weiter.
- **Icons** — keine.
- **Karte** — ✓ `InUse` rahmt beide Orte in je einer `Card`.
- **Texte T1–T5** — ✓ „vor 3 Tagen", „2 Std 5 Min", „am 14.09.2026", „seit
  02.09.2026", `de-DE`, keine Versalien.
- **Story im richtigen Pfad** — ✓.
- **In §11 auf v2 gesetzt** — ✓ `design-guidelines.md:420`.

### Nachbesserung (`a3bc067`) — was aus den vier Mängeln wurde

| # | Mangel aus dem ersten Durchgang | Behebung, wie nachgeprüft |
|---|---|---|
| M1 | Kalendertag verschob sich östlich von Berlin um einen Tag (`Asia/Tokyo` → `25.08.2026`, Jahreswechsel → `31.12.2025`) | `toDate` ankert auf 12:00 UTC. In fünf Zonen im Browser und sieben Zonen headless nachgemessen — überall dasselbe Datum. Der Fix ist sogar stärker als angekündigt: weil `Date.UTC` absolut ist, gilt er für **jede** Rechner-Zone, nicht nur für −11…+12. |
| M2 | `prefix` ohne Story | `InUse`, Spalte „Fällig": „am 14.09.2026" und „seit 02.09.2026" im DOM gelesen. |
| M3 | `InUse` ohne die von der Spec verlangte `Timeline` | `InUse` zeigt jetzt beide Orte in zwei Karten; die `Timeline` gruppiert nach Tagen und setzt ihre Uhrzeiten über denselben Baustein. |
| M4 | `format="age"` ohne Story | `Formats` hat eine fünfte Zeile („age — wie lange etwas schon liegt, ohne Datum davor"); der Story-Kommentar heißt jetzt „Fünf Arten zu sagen, wann". Einschränkung in Anmerkung 3. |

Nicht verlangt, aber mitgezogen: `Time.stories.tsx` war eine der fünfzehn
Dateien aus dem `<th>`/`<td>`-Befund und ist jetzt sauber (`grep -c "<th>\|<td>"`
→ `0`). Für die verbleibenden vierzehn gibt es
`docs/backlog/0091-tabellen-stories-ohne-th-td.md`.

### Anmerkungen des Abnehmenden

1. **`format.ts` trägt kein `@when`/`@instead`.** `formatTime`,
   `formatTimeFull`, `formatDuration` (und `formatAmount` aus 0032) sind über
   das Barrel öffentlich und haben JSDoc, aber nicht die zwei Zeilen — anders
   als die reine Funktion `parseAmount` (`AmountInput.tsx:25`). Nicht als
   Mangel gewertet (der Zuschnitt stellt `format.ts` neben die Komponente);
   geht als Befund ans Set.
2. **`<th>`/`<td>` im `div`-Tisch** — für diese Datei erledigt, für vierzehn
   weitere nicht: eigene Aufgabe 0091.
3. **`age` und `relative` sehen in der Story gleich aus.** Beide Zeilen in
   `--formats` benutzen denselben Wert `vorDreiTagen`, und bei drei Tagen
   liefern sie identisch „vor 3 Tagen" — der Unterschied entsteht erst nach
   einer Woche. Gemessen: 3 Tage alt → `relative` „vor 3 Tagen" / `age` „vor 3
   Tagen"; **13 Tage alt → `relative` „23.08.2026" / `age` „vor 13 Tagen"**;
   31 Tage alt → „05.08.2026" / „vor 31 Tagen". Genau die 13 Tage sind laut
   `0451ddb` der Median der offenen Klärungen, also der Grund, warum `age`
   existiert. Der Wert ist da und beschriftet, aber die Story beweist nicht,
   *wofür* er da ist — ein zweiter Beispielwert (13 Tage) würde es zeigen.
   Kleinigkeit, deshalb kein ✗; notiert, damit sie beim nächsten Anfassen
   mitgeht.
4. **Die absolute Zeit steckt nur im `title`** — bekannter Konflikt mit T7,
   in offener Frage 1 der Spec vermerkt und vom Owner in P24 so entschieden.
5. **Der `dateTime`-Wert eines Kalendertags folgt dem Anker nicht.**
   `Time.tsx:35` bildet ihn weiter mit `new Date(value)`, also
   `2026-08-26T00:00:00.000Z`, während der angezeigte Text über den 12:00-Anker
   läuft. Maschinenlesbar bleibt es der 26., also kein Fehler — aber für ein
   reines Datum wäre `dateTime="2026-08-26"` die genauere Angabe. Notiert, kein
   Mangel.

Abgenommen von / am: Claude (Abnahme-Agent), 2026-09-05 · Offene Punkte: keine
(die fünf Anmerkungen sind Befunde, keine Mängel dieser Aufgabe)
