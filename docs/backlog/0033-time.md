# 0033 · Time — ein Zeitpunkt, fünf Formate, plus Dauer

| | |
|---|---|
| Status | in Arbeit |
| Stufe | `primitives/` |
| Klassen-Test | „Ergäbe das auch in einer Versicherungs-App Sinn?" → ja, ein Zeitpunkt ist fachfrei |
| Quelle | `ludwig/app` `docs/topics/web-ui-offen.md` **P24**, Ziel des Owners vom 2026-09-03 |
| Ersetzt | 9 Datums-Formatierer (`formatGermanDate(Time)`, `fmtDate` ×2, `fmtDateTime`, `fmtDateShort`, `fmtDateFull`, `fmtTimestamp`, `formatCutoffDate`) und **83 `toLocale*`-Stellen in 64 Dateien**; dazu `TimeAgo` (v1) |
| Blockiert | P3 geht hier auf — die 83 Stellen rechnen ohne `timeZone` und liegen zwei Stunden daneben |
| Spec von / am | Claude, 2026-09-03 |

## Ziel

„Wann kam der Beleg?" beantwortet Ludwig in neun Formaten, und 83 Stellen
rechnen dabei ohne Zeitzone — die zeigen im Zweifel den Vortag. `Timestamp`
kann genau ein Format (`dd.MM. HH:mm`) und kein Datum ohne Uhrzeit; eine
Dauer („läuft seit 14 Min.") gibt es gar nicht, sie steht als Text im Code.

## Einordnung

- **Wiederverwenden:** `Timestamp` ist dieses Format, eines von fünfen. Es
  bleibt als Zell-Variante und setzt auf diesen Baustein auf.
- **Neu, weil:** Regel 3 — kein `@when` deckt Datum, Monat, relative Zeit und
  Dauer ab; kein Fachwort; 64 belegte Dateien.
- **Zuschnitt:** Familie in einer Datei — `Time` (ein Zeitpunkt) und
  `Duration` (eine Zeitspanne). Eine Dauer ist keine Uhrzeit: sie kommt als
  Sekunden, nicht als ISO-Wert; ein gemeinsamer Prop-Typ wäre eine Union, die
  an jeder Aufrufstelle erklärt werden müsste. Die Funktionen liegen in
  `src/ui/v3/format.ts` — dieselbe Datei wie bei 0032, ein Modul für beides.
- **Setzt auf:** `IsoDate`/`IsoDateTime` aus `src/ludwig/shared/dates.ts`.

## Schnittstelle

`Time`:

| Prop | Typ | Pflicht | Bedeutung | Nachweis (Story) |
|---|---|---|---|---|
| `value` | `IsoDate \| IsoDateTime \| Date \| null` | ja | Der Zeitpunkt; `null` ist der Gedankenstrich | `Empty` |
| `format` | `"date" \| "dateTime" \| "relative" \| "month"` | nein | Default `dateTime` | `Formats` |
| `length` | `"short" \| "medium" \| "long"` | nein | Nur bei `dateTime`: `26.08. 09:12` · `26.08.2026 09:12` · `Mittwoch, 26. August 2026 um 09:12` | `Lengths` |
| `size` | `"sm" \| "md"` | nein | wie bei `Amount` | `Sizes` |
| `prefix` | `string` | nein | „zuletzt", „seit" — ein Wort davor | `InUse` |

`Duration`:

| Prop | Typ | Pflicht | Bedeutung | Nachweis (Story) |
|---|---|---|---|---|
| `seconds` | `number \| null` | ja | Die Spanne | `Durations` |
| `size` | `"sm" \| "md"` | nein | | `Durations` |

`format.ts` liefert `formatTime(value, format, length)` und
`formatDuration(seconds)` — dieselben Regeln ohne React.

**Kann bewusst nicht:** Zeitzonen wählen (es ist immer Europe/Berlin, R3),
Zeiträume darstellen (das ist `DateRangeField`, 0024), rechnen.

## Verhalten

- **Immer `Europe/Berlin`** — das ist der Kern der Aufgabe. Ein Zeitpunkt ohne
  Zeitzone ist der Fehler, den P3 in 83 Stellen zählt.
- **Ein kalendarisches Datum verschiebt sich nicht.** `2026-08-26` ist ein
  Tag, kein Zeitpunkt: es wird aus seinen Teilen gesetzt, nicht über
  `new Date(...)` mit Zeitzonen-Umrechnung. Sonst wird aus dem Belegdatum je
  nach Ort der Vortag.
- **`relative`** zeigt „vor 3 Tagen" und trägt die absolute Zeit im `title`.
  T7 verlangt die absolute Angabe **neben** der relativen; der Owner hat in
  P24 den `title` dafür bestimmt. Wer die Zeit ohne Zeigen braucht, nimmt
  `dateTime` — siehe offene Frage 1.
- **`month`** ist für Achsen und Gruppenköpfe: `August 2026`.
- **Dauer**: unter 60 s mit einer Nachkommastelle (`4,2 s`), darunter keine
  Millisekunden; bis 60 min ganze Minuten (`14 Min`); darüber Stunden und
  Minuten (`2 Std 5 Min`). Kein „vor", kein „seit" — das ist `prefix` beim
  Aufrufer.
- Gerendert wird ein `<time dateTime=…>`, außer bei `Duration` — eine Spanne
  hat kein Datum.
- Server-Components. `relative` rechnet beim Rendern, nicht tickend: ein
  Zähler, der im Sekundentakt springt, zieht das Auge von der Arbeit weg (V12).

## Stories

Titel `v3/Primitives/Werte/Time`. Abgeleitet nach §6: 2 Zustände (gefüllt,
leer) + 2 Enums (`format`, `length`) + 0 Callbacks + 1 „im Einsatz"
+ 1 Rand (Jahreswechsel, Sommerzeit) + 1 zweiter Export (`Duration`) = 7.

| Story | Beweist |
|---|---|
| `Formats` | `date`, `dateTime`, `relative`, `month` untereinander |
| `Lengths` | `short`, `medium`, `long` am selben Zeitpunkt |
| `Durations` | 4,2 s · 14 Min · 2 Std 5 Min · 0 s |
| `Sizes` | `sm` und `md`, in einer Zeile fluchtend |
| `Empty` | `null` ist ein Gedankenstrich |
| `Edges` | reines Datum ohne Uhrzeit, Jahreswechsel, Sommer-/Winterzeit |
| `InUse` | in einer Tabellenzeile und in einer `Timeline` |

Nicht anwendbar: `LeerNachFilter`, `Laedt`, `Fehler`.

## Abnahme

Geprüft am 2026-09-05 von einem zweiten Agenten (nicht dem bauenden).
Geprüfter Stand: `e3c38e7`; `Time.tsx`, `Time.stories.tsx` und `format.ts`
sind im Arbeitsbaum unverändert. Gebaut in `a0636f7`, nachträglich erweitert
in `0451ddb` („Klärungs-Zeile", Format `age`). Storybook-Nachweise vom
laufenden Server auf Port 6107, gemessen in einer eigenen Chromium-Instanz
(1280 × 900); die Zeitzonen-Proben mit `timezoneId` bzw. `TZ=…`.

**Ergebnis: zurück auf `in Arbeit`.** Vier Punkte, davon einer der Kern der
Aufgabe. Alles andere steht.

### Story-Deckung — drei Lücken

`index.json` listet die sieben abgeleiteten Stories: `--formats`, `--lengths`,
`--durations`, `--sizes`, `--empty`, `--edges`, `--in-use`. Vollständig — aber
die Schnittstelle ist nicht gedeckt:

1. **`prefix` hat keine Story.** Die Schnittstellen-Tabelle nennt `InUse` als
   Nachweis; `Time.stories.tsx:133–168` benutzt `prefix` nirgends.
   `grep -rn "prefix=" src/ui/v3` findet die Prop nur an `Timestamp`
   (`Cells.stories.tsx:51,58`), `InputGroup` und `CommandPalette` — nie an
   `Time`. Die einzige Prop, die dem Zeitpunkt ein Wort voranstellt („zuletzt",
   „seit"), ist damit unbelegt.
2. **`InUse` zeigt keine `Timeline`.** Die Spec verlangt „in einer
   Tabellenzeile **und** in einer `Timeline`"; die Story hat nur die
   Tabellenzeile. Die `Timeline` ist die Stelle, an der `relative` und `prefix`
   zusammen auftreten — genau der ungedeckte Fall aus Punkt 1.
3. **`format="age"` hat keine Story.** Der Wert kam in `0451ddb` dazu
   (`format.ts:65,146`, `formatAge` `:159–165`) und ist in der App-Familie
   bereits im Einsatz (`entities/clarification/Clarification.tsx:192`).
   `Formats` zeigt vier der fünf Werte. Wie bei 0045/0046 ist die Erweiterung
   die Entscheidung der späteren Aufgabe — der fehlende Nachweis bleibt
   trotzdem eine Lücke im Baustein und gehört in `Formats`.

| Kriterium | Nachweis (Story-ID · Befehl · Beobachtung) | Ergebnis |
|---|---|---|
| `pnpm typecheck` und `pnpm build` grün | `pnpm typecheck` → `tsc --noEmit`, Exit 0, zu Beginn und am Ende. `pnpm build` bewusst **nicht** gelaufen (parallele Abnahmen schreiben nach `storybook-static`); der Lauf für diesen Stand war grün — „Storybook build completed successfully". | ✓ |
| Datei nach der Familie benannt, Story daneben, Titel in der richtigen Gruppe | `src/ui/v3/primitives/Time.tsx` und `Time.stories.tsx` nebeneinander; `Time.stories.tsx:6` setzt `title: "v3/Primitives/Werte/Time"`. Barrel: Gruppe „Werte" `index.ts:132`, Export `:134`. Die Familie liegt in einer Datei (`Time` + `Duration`), wie der Zuschnitt es sagt. | ✓ |
| Code englisch; `@when`/`@instead` an jedem Export | Zwei Exporte: `Time` (`Time.tsx:20`, `@when` Z. 14–16, `@instead` Z. 17–18) und `Duration` (`:55`, `@when` Z. 52, `@instead` Z. 53). Alles englisch außer Nutzertext. Siehe Anmerkung 1 zu `format.ts`. | ✓ |
| Kein Hex, kein px, keine lokale Label-Map; Status nur über Registry | `grep -nE "#[0-9a-fA-F]{3,8}\b\|[0-9]+px" src/ui/v3/primitives/Time.tsx src/ui/v3/format.ts` → keine Treffer. Maße in `v3.css:2025–2027`. Kein Status, keine Label-Map. | ✓ |
| Alle Stories oben vorhanden; ausgeschlossene Zustände begründet | Alle sieben da, `LeerNachFilter`/`Laedt`/`Fehler` begründet ausgeschlossen — **aber** drei Props/Werte ohne Nachweis, siehe „Story-Deckung". | ✗ |
| Prüfliste `design-guidelines.md` §9 durchgegangen | Punkt für Punkt unter dieser Tabelle. | ✓ |
| Im Browser angesehen (Storybook), nicht nur gebaut | Alle sieben Stories geöffnet, gemessen, als Screenshot gesehen. Konsole leer außer `--in-use`: vier React-Fehler „`<th>` cannot be a child of `<div>`" — Anmerkung 2. | ✓ |
| `2026-08-26` als reines Datum zeigt den **26.**, unabhängig von der Rechner-Zeitzone | **Zeigt in östlichen Zeitzonen den 25.** Story `--edges` in einer Chromium-Instanz mit `timezoneId`: Europe/Berlin → `26.08.2026`, America/Los_Angeles → `26.08.2026`, **Asia/Tokyo → `25.08.2026`**, **Pacific/Auckland → `25.08.2026`**; der Jahreswechsel `2026-01-01` wird dort zu `31.12.2025`. Ohne Browser reproduzierbar: `TZ=Asia/Tokyo npx tsx -e "…formatTime('2026-08-26','date')…"` → `25.08.2026`. Ursache: `toDate` (`format.ts:106–113`) baut den Kalendertag mit `new Date(y, m-1, d)` in der **Rechner-Zone**, und `DATE` (`:68–73`) rechnet ihn danach nach `Europe/Berlin` zurück — für jede Zone östlich von Berlin ein Tag zu früh. `formatTimeFull` (`:168–173`) erbt denselben Fehler, also verschiebt sich auch der `title`. Betrifft mittelbar `RawRecord` (0051), das Datumsspalten durch `Time` schickt. | ✗ |
| Jede Formatierung nennt `timeZone: "Europe/Berlin"` | `format.ts:16` `const TZ = "Europe/Berlin"`, gesetzt in allen fünf `Intl.DateTimeFormat`-Instanzen (`:68`, `:74`, `:75`, `:82`, `:90`). `grep -n "toLocale" src/ui/v3/format.ts` → keine Treffer. | ✓ |
| `relative` trägt die absolute Zeit im `title` | Story `--formats`, DOM-Probe: `<time class="v2time v2time--md" datetime="2026-09-02T11:38:03.914Z" title="Mittwoch, 2. September 2026 um 13:38">vor 3 Tagen</time>`. Der `title` steht an **allen** vier Formaten, nicht nur an `relative` (`Time.tsx:43`). | ✓ |
| Dauer: 4,2 s · 14 Min · 2 Std 5 Min stimmen auf die Sekunde | Story `--durations`, gelesene Reihe für `[0, 4.2, 42, 59.9, 60, 840, 3600, 7505, null]`: `0,0 s · 4,2 s · 42,0 s · 59,9 s · 1 Min · 14 Min · 1 Std · 2 Std 5 Min · —`. 7505 s = 125,08 min → gerundet 125 → 2 Std 5 Min. (Die Story-Tabelle oben schreibt „0 s"; gerendert wird „0,0 s" — richtig nach der Regel „unter 60 s mit einer Nachkommastelle".) | ✓ |
| Gerendert wird `<time dateTime=…>` | Story `--formats`, alle vier Elemente sind `TIME` mit gesetztem `datetime`; `Duration` ist bewusst ein `<span>` (`Time.tsx:63`) — eine Spanne hat kein Datum, wie die Spec sagt. Bei unlesbarem Wert bleibt `datetime` leer statt falsch (`--edges`, letzte Zeile). | ✓ |
| `Timestamp` setzt auf `Time` auf, statt ein zweites Mal zu formatieren | `Cells.tsx:2` importiert `formatTime` und `formatTimeFull`, `:89–91` ruft sie („The short form of `Time` (P24) — this cell is its table-shaped variant"). Kein zweites `Intl.DateTimeFormat` in `Cells.tsx`. | ✓ |
| `formatTime`/`formatDuration` sind ohne React aufrufbar | `format.ts` hat genau einen Import, und der ist ein Typ (`:1`). Headless: `npx tsx -e "…"` → `26.08.2026, 09:12 \| 26.08.2026 \| August 2026` und `4,2 s \| 14 Min \| 2 Std 5 Min \| —`, dazu `react loaded? false`. | ✓ |

### Prüfliste §9, Punkt für Punkt

- **Stufe und Importe** — ✓ `primitives/`, importiert nur `../format`.
- **Ersetzt ihr v1-Gegenstück** — offen (App).
- **Kein Hex, kein px, keine Label-Map** — ✓.
- **Text links, Zahlen rechts mit `tnum`, nichts zentriert** — ✓ für den
  Baustein: `tabular-nums` an `.v2time` gemessen, Datum links. **Befund in
  `--in-use`:** vier Spaltenköpfe auf `text-align: center`, Anmerkung 2.
- **Zeilenhöhe ≤ `.v2tbl__row`** — ✓ `.v2time` bringt keine eigene mit.
- **Farbe** — ✓ keine; ein Zeitpunkt trägt keine Kritikalitätsstufe.
- **Fünf Zustände** — ✓ gefüllt und leer gebaut, drei begründet ausgeschlossen.
- **Kontrast** — ✓ erbt die Textfarbe; `null` in `--color-text-muted`.
- **Tastatur / Hover** — nicht anwendbar. **Anmerkung 3:** die absolute Zeit
  hängt allein am `title` und ist damit nur per Maus erreichbar; die offene
  Frage 1 der Spec vermerkt den Konflikt mit T7 ausdrücklich. Kein Mangel
  gegen die Spec, aber der Punkt lebt weiter.
- **Icons** — keine.
- **Karte** — nicht anwendbar.
- **Texte T1–T5** — ✓ „vor 3 Tagen", „2 Std 5 Min", `de-DE`, keine Versalien.
- **Story im richtigen Pfad** — ✓.
- **In §11 auf v2 gesetzt** — ✓ `design-guidelines.md:420`.

### Was zu tun ist

1. **Kalendertag ohne Zonen-Umweg formatieren.** Ein Tag ist keine Instanz:
   `2026-08-26` muss aus seinen Teilen ausgegeben werden (oder mit
   `timeZone: "UTC"` bei UTC-Mitternacht), nicht als lokale Mitternacht, die
   danach nach Berlin gerechnet wird. Gilt für `formatTime(…,"date")`,
   `formatTimeFull` und den `dateTime`-Wert in `Time.tsx:35`. Danach `--edges`
   in einer östlichen Zone gegenprüfen — das ist der Nachweis, den das
   Kriterium verlangt.
2. **`prefix` in einer Story zeigen** — am besten in `InUse`, wie die
   Schnittstellen-Tabelle es vorsieht.
3. **`InUse` um die `Timeline` ergänzen**, wie die Spec sie fordert.
4. **`Formats` um `age` erweitern** und die Schnittstellen-Tabelle dieser Spec
   um den fünften Wert nachziehen, damit Spec und Code wieder dasselbe sagen.

### Anmerkungen des Abnehmenden

1. **`format.ts` trägt kein `@when`/`@instead`.** `formatTime`,
   `formatTimeFull`, `formatDuration` (und `formatAmount` aus 0032) sind über
   das Barrel öffentlich und haben JSDoc, aber nicht die zwei Zeilen — anders
   als die reine Funktion `parseAmount` (`AmountInput.tsx:25`). Nicht als
   Mangel gewertet (der Zuschnitt stellt `format.ts` neben die Komponente),
   aber nachzuholen, wenn die Datei ohnehin für Punkt 1 angefasst wird.
2. **`<th>`/`<td>` im `div`-Tisch — ein Befund für das Set.**
   `Table`/`HeadRow`/`Row` sind CSS-Grid-`div`s (`Table.tsx:102`, `:118`);
   `Time.stories.tsx:136–166` füllt sie mit `<th>`/`<td>`. Folge: React-Fehler
   in der Konsole und `text-align: center` auf den Spaltenköpfen aus dem
   UA-Stylesheet (V3-Verstoß). Betroffen sind **15 Story-Dateien** unter
   `src/ui/v3`, darunter bereits abgenommene; 15 weitere machen es richtig mit
   `<div>`, ebenso `DataTable`/`Log`/`ComparisonTable` im Produktivcode.
   Gehört als eigene Aufgabe ans Set, nicht in diese.
3. **`Time` mit `null` verliert seine Klasse.** `Time.tsx:34` gibt
   `<span class="v2muted">—</span>` ohne `v2time` zurück, `Duration` behält sie
   (`:63`). In einer Spalte mit Lücken fluchtet der Strich damit nicht mit den
   Ziffern darüber. Kleinigkeit, aber V3.

Abgenommen von / am: — · Offene Punkte: die vier unter „Was zu tun ist"
