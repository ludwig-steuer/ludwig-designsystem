# Befunde für `ludwig/app` — was drüben gebaut werden muss

Diese Datei ist die **Übergabe an den Entwicklungsagenten der App**. Sie
sammelt, was beim Bauen des Design-Systems an `ludwig/app` aufgefallen ist und
dort erledigt werden muss — an einem Ort, statt verstreut über 20
Backlog-Dateien und zwei Entitätsprofile.

**Wie sie entsteht:** Jede Spec und jedes Entitätsprofil hält seine Befunde
weiter bei sich — dort stehen sie im Zusammenhang, und dort findet sie der
Abnehmende. Hier stehen sie **zusätzlich**, in der Form, in der ein fremder
Agent sie abarbeiten kann: was fehlt, wo, was zu tun ist, und was hier darauf
wartet. Steht ein Befund hier, ist er noch nicht erledigt.

**Wie sie gepflegt wird:** Wer eine Spec schreibt oder eine Entität
analysiert, trägt seine App-Befunde am Ende **auch hier** ein — eine Zeile je
Befund, mit Quelle. Wer einen Befund in der App erledigt, streicht ihn hier
und nennt den Commit. Die Regel steht in `docs/backlog/README.md`.

**Nummern:** Das Design-System vergibt ab **L-200**, der Entwicklungsagent der
App zählt bei **L-100** weiter (Owner-Entscheid, 2026-09-07). Lücken sind
erlaubt — eine Nummer wird nie zweimal vergeben und nie nachträglich
verschoben.
**Was hier nicht hingehört:** Befunde über das Design-System selbst („Befund
beim Bauen" in den Specs) — die bleiben in ihrer Aufgabe. Und die
Ablösung einer App-Komponente durch einen v3-Baustein ist kein Befund,
sondern der Migrationsschritt; er steht in Abschnitt E.

| | |
|---|---|
| Stand | 2026-09-08 |
| Quellen | `docs/backlog/*.md` (Abschnitte „Befunde für `ludwig/app`", Kriterien „offen (App)") · `docs/entitaeten/*.md` |
| Erhoben von / am | Claude, 2026-09-04 — maschinell aus allen Aufgaben und Profilen extrahiert, nicht aus dem Gedächtnis |

---

## A · Fehlende Typen und Ableitungen in `src/ludwig/`

Der Spiegel (`scripts/sync-ludwig.sh`) ist die Vorgabe für jede
Komponenten-Schnittstelle: **eine Komponente, deren Props sich dort nicht
bedienen lassen, ist falsch geschnitten** (`spec-schreiben` §5). Fehlt ein
Typ, definiert das Design-System ihn strukturell deckungsgleich selbst und
meldet ihn hier — es erfindet keine Begriffe.

| # | Was fehlt | Wo es hingehört | Heute stattdessen | Quelle |
|---|---|---|---|---|
| **L-265** | `agentRunId`, `exportBatchId`, `datevDocumentLinkSystem`/`…Guid` am `RecurringRule` | `modules/recurring-rules/domain/rule.ts` | gar nicht — der gespiegelte Typ kennt die vier Felder nicht, obwohl das Entitätsprofil sie als Ränge 27 und 28 führt (Buchungslauf 0 %, Buchungszyklus 7 %, DMS-Beleglink 90 %). `RecurringRuleFacts` lässt die Zeilen deshalb weg; sie zu zeigen hieße, den Typ zu erfinden (`spec-schreiben` §5). **Entweder gehören die vier in den Domänen-Typ, oder die Ränge 27/28 aus dem Profil** — heute kann keine Form sie zeigen | `docs/backlog/0134-recurring-rule-facts.md`, Abschnitt „Gebaut" Nr. 3 |
| ~~**L-01**~~ | Parser für deutsche Geldeingaben („1.234,56", „1234.56", „1 234,56") | neben `formatMoney` in `shared/money.ts` | `AmountInput` trägt ihn mit `ponytail:`-Kommentar — **erledigt 2026-09-07 (App-Seite): Commit `52914c45`.** `parseGermanAmount` steht in `shared/money.ts`; `AmountInput` rechnet nicht mehr selbst und prüft nur noch die Form, damit „12,3,4" im Feld ungültig bleibt statt still zu 123,4 zu werden | 0019 |
| ~~**L-02**~~ | `EVENT_KINDS` + `EVENT_KIND_LABEL`, dazu die Registry-Achse `ereignis_art` | `modules/accounting-cases/domain/case.ts` | Arten nur im DB-CHECK und im `switch` von `EventIcon` — der kennt `contract_received`/`recurring`, die der CHECK nicht hat, und `open_item_carryover` nicht. — **erledigt 2026-09-06 (App-Seite): Commit `c5d1dffd`.** `EVENT_KINDS` und `EventKind` liegen in `accounting-cases/domain/case.ts`, die Achse `ereignis_art` in der Registry. `Timeline` und `CaseTimeline` lösen ihre lokale Ableitung damit ab | 0023, 0040 |
| ~~**L-03**~~ | `OpenItemLink`-Interface | `src/ludwig/` | in der Komponente — **erledigt 2026-09-07 (App-Seite): Commit `52914c45`.** `OpenItemLink` steht in `datev-truth/domain/open-item.ts`; 0026 bleibt zurückgestellt, der Typ ist da, wenn die Aufgabe drankommt | 0026 |
| ~~**L-04**~~ | `DatevSnapshot` + `BaselineLevel`-Union | `src/ludwig/` (Wortwahl der drei Stufen steht im GLOSSARY) | in der Komponente — **erledigt 2026-09-07 (App-Seite): Commit `222c8d5a`.** `datev-mirror` und `datev-truth` haben je ein `domain/`; der Spiegel nimmt `snapshot.ts` und `open-item.ts` (0027, 0029) | 0027 |
| ~~**L-05**~~ | `OpenItem`-Interface **und** `openItemAgeBucket({ dueDate, asOf })`, analog `expectationMaturity` | `modules/accounting-cases/domain/` | die Komponente nimmt den Bucket als Prop, rechnet ihn nicht — **erledigt 2026-09-07 (App-Seite): Commit `222c8d5a`.** `openItemAgeBucket()` samt Klassen und Wörtern steht in `datev-truth/domain/open-item.ts`; 0029 liest sie von dort, rechnet weiter nichts selbst | 0029 |
| ~~**L-06**~~ | Kanonisches Zeilenmodell des Buchungssatzes | Domäne statt `ui/booking/types.ts` (`BookingLineVM`) | `JournalLine` im Design-System, strukturell deckungsgleich. — **erledigt 2026-09-06 (App-Seite): Commit `e895647b`.** das Sichtmodell steht in `modules/entries/domain/journal-entry-vm.ts` (`BookingLineVM`, `JournalEntryVM`) und ist gespiegelt | 0044 |
| ~~**L-07**~~ | `JournalEntryVM.currency` als `Currency` statt `string` | `modules/entries/domain/` | die Aufrufstelle braucht einen Cast. — **erledigt 2026-09-06 (App-Seite): Commit `e895647b`.** `JournalEntryVM.currency` ist `Currency` | 0044 |
| ~~**L-08**~~ | `TimelineEventVM` / `CaseEvent` spiegeln | heute `domain/overview-vm.ts` bzw. `infrastructure/` | nicht gespiegelt; die drei Sichten aus 0040 sind der Vorschlag — **erledigt 2026-09-07 (App-Seite): Commit `52914c45`.** `case-event.ts` liegt in `accounting-cases/domain/`; die Wörter der Ereignisarten nehmen Timeline und CaseTimeline aus der Achse `ereignis_art`, `kindLabels` ist zur Überschreibung geworden und die lokale Map der Stories ist weg | 0040 |
| ~~**L-09**~~ | `ClarificationVM` spiegelbar machen — die Datei importiert `@/ui/booking`, deshalb überspringt sie `sync-ludwig.sh`. **Ein reines Domänen-VM hat keine UI-Abhängigkeit** | `modules/accounting-cases/domain/overview-vm.ts` | nicht gespiegelt. — **erledigt 2026-09-06 (App-Seite): Commit `e895647b`.** `ClarificationVM` ist importfrei und wird gespiegelt | 0059 |
| ~~**L-10**~~ | `CLARIFICATION_QUESTION_TYPE_LABEL` (9 Werte im Bestand) | neben `CLARIFICATION_TYPES` in `domain/case.ts` | `humanizeType()` in `ClarificationsBanner` — Unterstriche zu Leerzeichen — **erledigt 2026-09-07 (App-Seite): Commit `34b5cdc6`.** `CLARIFICATION_QUESTION_TYPE_LABEL` kennt fünfzehn Typen; die Klärungskarte nimmt den rohen `questionType` und schlägt das Wort dort nach | clarification B1 |
| ~~**L-11**~~ | `MODULE_LABEL` in die Domäne heben; es kennt `agent`, `web` und `datev-mirror` nicht — **78 % des Bestands** | `domain/` | lebt in `ClarificationsBanner.tsx` — **erledigt 2026-09-07 (App-Seite): Commit `34b5cdc6`.** `CLARIFICATION_MODULE_LABEL` steht in der Domäne; die Karte nimmt `sourceModule` roh | clarification B2 |
| ~~**L-12**~~ | `ClarificationAnswerKind` um `document_upload` erweitern — TS kennt vier Werte, der DB-CHECK fünf, acht Zeilen tragen den fünften | `modules/invoices/domain/invoice.ts` | `ClarificationEditor` weitet die Union lokal und begründet es — **erledigt 2026-09-07 (App-Seite): Commit `34b5cdc6`.** `ClarificationAnswerKind` kennt `document_upload`; die lokale Weitung in der Karte ist weg | clarification B9 |
| ~~**L-13**~~ | `AccountFactsVM` — die Fakten eines Kontos in einem Wirtschaftsjahr | `modules/accounts/domain/` | die App setzt sie zweimal zusammen: `AccountLedgerDrawer.tsx:88–97` und `accounts/[accountNumber]/page.tsx:302–318`. — **erledigt 2026-09-06 (App-Seite): Commit `c0e85bfa`.** `AccountFactsVM` liegt in der Domäne | 0066 |
| ~~**L-14**~~ | `AccountEntry` — **eine** Bewegung auf einem Konto, mit der Herkunft als Wert | `modules/accounts/domain/` | zwei getrennte Typen: `AccountLedgerRow` (snake_case, Beträge `string`) und `TruthAccountEntryRow` (camelCase, `number`); keiner trägt die Herkunft, die Vereinigung ist heute die Tab-Umschaltung. — **erledigt 2026-09-06 (App-Seite): Commit `c0e85bfa`.** `AccountEntry` samt `accountEntryFromLedgerRow` liegt in `accounts/domain/account-entry.ts` | 0067 |
| **L-15** | `PageRequest` um `sort`/`dir`, plus Whitelist erlaubter Sortierschlüssel je Spalte in jeder `list*ForClient` | `shared/pagination.ts` | `parsePageRequest` kennt nur `page` und `size` — **keine der 13 Listenseiten sortiert** | 0057 |
| ~~**L-16**~~ | Server Actions, die `ids: string[]` nehmen (Freigabe in `cases` und Stapelabnahme) | die betroffenen Module | Freigabe läuft je Zeile; `DataTable` bringt die Auswahl mit, die Aktion fehlt (V11) — **wartet auf Welle 1, Punkt 1** (Owner-Reihenfolge, 2026-09-07) | **Erledigt 2026-09-08, App-Commit `5a412eeb`.** `acceptCaseProposals({ caseIds })` in `accounting-cases/application/close-actions.ts`, höchstens 500 je Aufruf, Teilerfolg als `failed[]`. Damit hat die Massenaktion der Abnahmeliste ihr Ziel — `DataTable` bringt die Auswahl mit, die Aktion gibt es jetzt. |
| ~~**L-36**~~ | `documentCounterparty({ docDirection, vendorName, customerName, classCounterpartyName })` — **ein** Weg zum Namen des Gegenparts | `modules/source-docs/domain/` | drei Wege: `PartnerCell` rechnet aus Richtung + vendor/customer, `BelegeTab` nimmt `class_counterparty_name`, der v3-Drawer beschriftet ihn „Lieferant" — **erledigt 2026-09-07 (App-Seite): Commit `7184a8ac`.** `documentCounterparty()` ist der eine Weg. Für das Set ändert sich am Bau nichts — die Familie nimmt den Namen fertig vom Aufrufer und leitet selbst nichts ab; die falsche Beschriftung „Lieferant" im Drawer heißt jetzt „Gegenpart". **Anmerkung:** die Funktion ist nicht spiegelbar, weil sie in einer Datei mit einem Drizzle-SQL-Ausdruck steht — sollte sie hier je gebraucht werden, muss sie eine eigene Datei bekommen · **Ganz erledigt 2026-09-07: `39096d2d`** — `documentCounterparty()` hat eine eigene Datei ohne Drizzle-Import und wird gespiegelt. **Aufgerufen wird sie hier trotzdem nicht, und das ist richtig:** `SourceDocumentVM` trägt den fertigen `counterparty`, nicht die vier Rohfelder; die Kaskade läuft, bevor das Anzeigemodell entsteht. Die Komponente leitet nichts ab (`spec-schreiben` §5). Unser Anteil am Befund war die Beschriftung „Lieferant" im Drawer — sie heißt „Gegenpart" | `entitaeten/source-document.md` B6 |
| ~~**L-37**~~ | Registry-Achse `beleg_charakter` für `document_kind` (`original` · `credit_note` · `self_billing` · `refund` · `unknown`) | `status-registry.ts` bzw. die Achsen-Quelle der App | `DOCUMENT_KIND_LABEL` als Label-Map; die Belegliste zeigt den Wert als Badge ohne Achse. Vier Einordnungs-Achsen, drei in der Registry — **erledigt 2026-09-07 (App-Seite): Commit `7184a8ac`.** Die Achse `beleg_charakter` gibt es; `SourceDocumentClass` zeigt den Charakter als `StatusBadge` statt als `Badge` mit Rohwort | `entitaeten/source-document.md` B5 |
| ~~**L-47**~~ | `SourceDocCompletionVia` — die sechs Werte von `client_source_docs.completed_via` (`booking`, `case_closed`, `import`, `superseded`, `manual`, `no_booking_required`) haben keinen TS-Typ | `modules/source-docs/domain/` | 0074 definiert die Union lokal, strukturell deckungsgleich mit dem DB-CHECK — **erledigt 2026-09-07 (App-Seite): Commit `7184a8ac`.** `SOURCE_DOC_COMPLETION_VIA` steht in `source-docs/domain/document-form-labels.ts`; die lokale Union in 0074 ist ein Re-Export | 0074 |
| ~~**L-48**~~ | **Die Status-Registry spiegelbar machen.** `status-registry.ts` importiert `BadgeKind` aus `@/ui/components`; stattdessen ein eigener `export type StatusKind = "info" \| "success" \| "warning" \| "danger" \| "neutral"`, den `Badge` der App aliast. Danach hat die Datei keinen Import mehr und `sync-ludwig.sh` kann sie spiegeln. **Owner-Entscheid 2026-09-04: die Registry bleibt in der App, das Set löscht seine Kopie** | `ui/status/status-registry.ts` (Pfad bleibt) oder `core/status/` — die App entscheidet | zwei Registries, bereits gegabelt (42 Diff-Zeilen). — **erledigt 2026-09-05 (App-Seite): Commit `cf681257`.** **Der Pfad bleibt** `apps/web/src/ui/status/status-registry.ts` — Entscheid der App: 81 Konsumenten liegen im selben Ordner, und spiegelbar macht die Import-Freiheit, nicht der Ordner | 0080 |
| ~~**L-49**~~ | Achse `actor_kind` (`platform_audit_events.actor_kind`: `user` · `system` · `api` · `cli` · `agent`, alle `neutral`) übernehmen — Block `ACTOR_KIND` aus `packages/designsystem/src/ui/v3/patterns/status-registry.ts`, samt Deckungsgleichheits-Test, `AXIS_LABEL`, `AXIS_SOURCE`, `ENTITY_ICON` | `status-registry.ts`, `entity-icons.ts`, `__tests__/status-registry.test.ts` | nur im Set. — **erledigt 2026-09-05 (App-Seite): Commit `cf681257`.** Achse samt Deckungsgleichheits-Test und Icons übernommen | 0053, 0080 |
| ~~**L-50**~~ | **Eine** Ableitung `confidenceLevel(value)` Rohwert → Stufe, spiegelbar, mit den Schwellen des Python-SSOT `confidence_to_band` (GLOSSARY „Confidence band"). Dazu der Entscheid, ob die Achse `konfidenz` (vier Farben) auf die fünf Band-Wörter geht | `shared/` | es waren **vier** Schwellensätze, nicht drei — `ConfidenceBand` 85/70/55/40 war der vierte. — **erledigt 2026-09-05 (App-Seite): Commit `cf681257`.** Jetzt **eine** Datei `shared/confidence.ts` mit `confidenceBand` (fünf Wörter), `confidenceLevel` (vier Stufen) und `confidencePercent`. **Offen bleibt die Owner-Frage**, ob die Achse `konfidenz` bei vier Stufen bleibt oder auf die fünf Band-Wörter geht — Stand: vier, und der v3-`Confidence` folgt der Achse | 0078 |
| ~~**L-51**~~ | Sieben handgeschriebene Status-Legenden sind sieben fehlende Achsen (Z2) — die achte, `configuration/integrationen/page`, ist mit dem Seiten-Rückbau vom 2026-09-05 weg: `admin/tenants/[tenantId]/page`, `admin/tenants/[tenantId]/clients/[clientId]/page`, `PaymentChannelActivitySection`, `VorsteuerTab`, `BridgeHealthStatus`, `AgentTokenManager`, `DatevExportSection`. Dazu 20 `hint`-Texte an `StatusHeader`-Aufrufen gegen `AXIS_LABEL`/`AXIS_SOURCE` prüfen — der v3-`StatusHeader` hat kein `hint` | `status-registry.ts` | `legend={[…]}` von Hand  — **erledigt 2026-09-05 (App-Seite): Commit `cf681257`.** Es wurden **sieben** neue Achsen (`VorsteuerTab` trug zwei), und `DatevExportSection` war gar keine neue Achse, sondern eine veraltete Handkopie von `zyklus_stapel` | 0077 |
| ~~**L-44**~~ | ~~`SourceDocType` fehlt der Wert `declaration`~~ — **erledigt 2026-09-04 (App-Seite): Absicht.** Die Union ist der Typ des Mapping-**Ergebnisses**, und `declaration` hat keinen Schreiber; lesend sind sieben Werte richtig, `SOURCE_DOC_TYPE_LABELS` führt alle | nichts zu tun. 0074 weitet die Union lokal — bestätigt | 0074 |
| ~~**L-17**~~ | `BATCH_LOG_VIEWS` als allgemeines `LOG_VIEWS` — es trägt die Wörter aus Z6, hängt aber am Stapel | `audit-log/domain/`, der Stapel importiert | stapelgebunden — **erledigt 2026-09-07 (App-Seite): Commit `52914c45`.** `LOG_VIEWS` und `LOG_VIEW_DEPTH` stehen in `shared/log-views.ts`; `LogBrowser` nimmt die Wörter von dort, `viewLabels` bleibt Überschreibung | 0054 |
| ~~**L-52**~~ | `caseDisplayTitle({ title, kind, counterpartyName })` — **ein** Weg zum Anzeigenamen des Sachverhalts: `title`, sonst `<Art>: <Gegenpart>`, sonst `<Art>` | `modules/accounting-cases/domain/case.ts` | **Korrigiert im Prüflauf 2026-09-05: es sind vier verschiedene Regeln, nicht dreimal dieselbe** — `PortalCaseList.tsx` Z. 254–256, der Kopf, der Partner-Reiter (`title ?? summary ?? „—"`) und der Tooltip der Liste. Und eine davon **verliert Nutzerdaten**: `buildCaseOverview()` (`overview-vm.ts` Z. 708–710) liest `detail.title` nie, ein vom Nutzer gesetzter Titel ist auf der Detailseite unsichtbar. Es ist Rang 1 aller fünf Sachverhalts-Formen. — **erledigt 2026-09-06 (App-Seite): Commit `44f9cd4e`.** `caseDisplayTitle` ist Kanon — Titel, sonst Art und Gegenpart, sonst die Art allein. 0095 nimmt ihn aus dem Spiegel | `entitaeten/accounting-case.md` B1 |
| **L-53** | Entweder Registry-Achse für `client_accounting_case.kind` oder die Farbe weg | `status-registry.ts` bzw. `CaseKindCell` | `CaseKindCell` färbt `recurring_charge` als `warning` und alles andere als `info`; `CASE_KIND_LABEL` ist laut eigenem Kommentar bewusst kein Status („keine Farbe, keine Übergänge"). Beides zusammen geht nicht — der v3-`CaseRow` zeigt die Art bis dahin ohne Farbe. **Prüflauf 2026-09-05: es trifft ein zweites Mal zu** — `CloseCasesPanel` färbt `recurring_charge` mit fest verdrahteten Hex-Werten, ganz ohne `Badge` | `entitaeten/accounting-case.md` B2 |
| **L-54** | `lifecycleStatus` in die Queries der drei Listen aufnehmen, die `CaseCell` zeigen | `documents/page.tsx`, `KontoauszugView.tsx`, `StuckDocumentsTable.tsx` | `ui/case/CaseCell.tsx` trägt nur die Nummer. **Korrigiert im Prüflauf 2026-09-05: nur zwei der drei Listen rufen die geteilte Zelle** — `KontoauszugView.tsx` Z. 134 hat eine eigene (0/1/n Fälle, „offen"-Pille, Betrag je Fall) und gehört mit abgelöst. Wer im Kontoauszug sieht, dass eine Zahlung an Sachverhalt 2026-0412 hängt, weiß nicht, ob der noch offen ist — Rang 2 fehlt an der kleinsten Form. Der v3-`CaseCell` nimmt ihn mit | `entitaeten/accounting-case.md` B3 |
| ~~**L-56**~~ | Die **Anzeige**-Seite der Kontoauszugsposition spiegeln: der Zeilen-Typ und fünf reine Ableitungen — `extractSepaTags()` und `derivePurposeParts()` (SVWZ statt Rohblock; ohne die erste läuft die zweite nicht), `deriveZ()`/`restOf()` (Zuordnungs-Zustand Z0–Z3), `datevMatchTitle()` (Match-Stufe → Haken), `deriveCaseIndicators()` — dazu `PURP_LABELS` | `modules/bank-transactions/domain/` | **Korrigiert im Prüflauf 2026-09-05:** der Ordner existiert, gespiegelt ist aber nur die **Import**-Seite (`BankTransactionRow` als Parser-Ausgabe, `raw-sheet`). Alle fünf Ableitungen liegen in `ui/` bzw. `infrastructure/` der App; eine v3-Komponente kann sich dort nicht bedienen und definiert sie strukturell deckungsgleich lokal. `resolveEventBookingState` ist **kein** Befund — das gibt es in v3 schon. — **erledigt 2026-09-06 (App-Seite): Commit `cc141f7b`.** die Ableitungen der Kontoauszugsposition (SEPA-Tags, Zweck) liegen in `bank-transactions/domain/`. **Rest im Set (2026-09-06):** der **Anzeige**-Typ (`BankTransactionAssignmentRow`) ist drüben Infrastruktur und bleibt es; das Set definiert ihn einmal in `entities/bank-transaction/bank-transaction.ts` (`…DetailData ⊃ …RowData ⊃ …CellData`) statt dreimal. | `entitaeten/bank-transaction.md` B2 |
| ~~**L-57**~~ | Registry-Achse für `client_bank_transactions.match_stage` — zwölf Werte, acht „gematcht", vier „offen" | `status-registry.ts` | ein grüner Haken plus handgeschriebener Titel (`datevMatchTitle`, `DATEV_MATCHED_STAGES` als Set von Hand). Die vier offenen Klassen sind unsichtbar, `beyond_bookings` allein 328 von 1281. Nicht zu verwechseln mit `mirror_match` — das ist `client_datev_mirror_entries.match_state`. — **erledigt 2026-09-06 (App-Seite): Commit `cc141f7b`.** die Achse heißt **`bank_match_stage`**, `beyond_bookings` trägt `info`. Die Haken-Übergangsregel in 0101/0102 entfällt | `entitaeten/bank-transaction.md` B3 |
| **L-66** | Je eine **Registry-Achse für die 17 `StatusHeader` mit `hint`**, hinter denen heute keine Achse steht | `status-registry.ts` | `AccountLedgerDrawer` (Buchungszustand), `bankkonten/page` (Kontoauszug, Gate 1a), `datev/page` (fünf: Lauf, Festschreibung, Prüfung, Abgleich, WJ-Status), `accounts/[accountNumber]` (drei), `documents/page` (Erledigt), `opos` (Ausgleich), `CaseDatevTruthTab` (drei), `StuckDocumentsTable`, `CasePlausibilityTab` (zwei). Der v3-`StatusHeader` hat **kein** `hint` (0077) — ohne Achse löscht der Import-Tausch ihren einzigen Erklärtext. **Vorbedingung für A5 der App** | 0077 · `ludwig-worker` 2026-09-05 |
| **L-67** | Registry-Achse **oder** Label-Konstante für `ProvenanceSource` (`"ai" \| "manual"`) — woher ein Buchungsfakt stammt | `status-registry.ts` bzw. `modules/contracts/domain/contract.ts` | Den Typ gibt es im Spiegel, die **Wörter** nicht: `ContractDetail.tsx` (`ProvMark`, Z. 96–120) schreibt sie in die App-Komponente — „geprüft" für den Menschen, „KI · 87 %" für die Extraktion, dazu „unsicher" und „fehlt". `SourceDocumentFacts` (0076) hatte sie zuerst neu erfunden („von Hand" / „automatisch gelesen"); jetzt sind sie **wörtlich kopiert**, was die Dublette nur sichtbar macht, statt sie zu beheben. Mit einer Achse fällt die Funktion `provenance()` weg und ein `StatusBadge` tritt an ihre Stelle | 0076 |
| ~~**L-68**~~ | `CaseDetail` liegt in `infrastructure/case-detail-queries.ts` statt in `domain/` — der Spiegel nimmt daraus nichts — und trägt elf Felder nicht, die die Fakten des Sachverhalts brauchen: `title`, `disposition`, `openClarificationsCount`, `exportStatus`, `counterpartySide`, `batchOposReference`, `createdByKind`/`createdByLabel`, `expectedInterval`, `agentRunId`, `exportBatchId`. Dass `title` fehlt, ist die **Wurzel von L-52** | nach `domain/` heben und ergänzen (Muster L-08/L-09); bis dahin ein lokales `CaseFactsVM`, Präzedenz `AccountFactsVM` (L-13) — **erledigt 2026-09-07 (App-Seite): Commit `18ddaa28`.** Der Typ liegt in `domain/case-detail.ts`, wird gespiegelt und trägt alle elf Felder; `CaseFactsVM` ist seither `Partial<CaseDetail>` plus vier Pflichtfelder, die lokale Fassung ist weg (0097, 0098) | lokales `CaseFactsVM` (0097, 0098), Präzedenz `AccountFactsVM` | 0097, Freigabe 2026-09-06 |
| ~~**L-69**~~ | `CaseListItem.counterpartyPartnerId` fehlt — die Zeile kann den Gegenpart nicht verlinken, obwohl **47 %** der Sachverhalte einen aufgelösten Partner haben | Feld ergänzen — **erledigt 2026-09-07 (App-Seite): Commit `a094826e`.** Der Spiegel führt das Feld; die Stories von 0096 verlinken den Gegenpart jetzt über die Partner-Id, ein Fall bleibt bewusst ohne | `counterpartyHref` als Funktion des Aufrufers (0096) | 0096, Freigabe 2026-09-06 |
| ~~**L-70**~~ | `ExpectationRow` samt `ExpectationAudience`, `DueSource` und `ExpectationDirection` liegt in `application/expectation-core.ts` | nach `modules/accounting-cases/domain/` heben (Muster L-09) | `application/expectation-core.ts` — **erledigt 2026-09-07 (App-Seite): Commit `3048389d`.** `ExpectationRow` liegt in `accounting-cases/domain/expectation.ts`; 0025 baut sein Sichtmodell darauf | 0025, Freigabe 2026-09-06 |
| ~~**L-71**~~ | `DOCUMENT_NUMBER_SOURCE_LABEL` und `_STATE_LABEL` sind eine **lokale Map** in `CasePlausibilityTab.tsx:40`; `DATEV_MAX_BELEGFELD1 = 36` steht nirgends als Konstante | Labels ins Domain-Modul, die Grenze nach `core/datev/field-limits.ts`. Bis dahin nimmt `DocumentNumberField` die Labels als Prop | lokale Map in `CasePlausibilityTab.tsx:40`; `DocumentNumberField` nimmt die Labels als Prop — **erledigt 2026-09-07 (App-Seite): Commit `362325b2`.** `DOCUMENT_NUMBER_STATE_LABEL` steht in der Domäne, `DATEV_MAX_BELEGFELD1` in `core/datev/field-limits.ts`; 0014 importiert beides, die Label-Props sind zu Überschreibungen geworden | 0014, Freigabe 2026-09-06 |
| ~~**L-73**~~ | **L-05 hängt am falschen Modul:** die Altersklassen der offenen Posten gehören zu `datev-truth`, nicht zu `accounting-cases`, und `datev-truth` hat kein `domain/`. Die Union ist zeichengleich festzuschreiben: `notDue \| d1_30 \| d31_60 \| d61_90 \| d90plus`. Dazu gehört **0029 als Wartender in L-66** | `domain/` anlegen, L-05 dorthin, Union festschreiben, L-66 ergänzen — **erledigt 2026-09-07 (App-Seite): Commit `222c8d5a`.** Die Altersklassen hängen jetzt an `datev-truth`, nicht an `accounting-cases` | 0029, Freigabe 2026-09-06 |
| ~~**L-74**~~ | **Übergänge liegen nirgends als Daten.** Vier von 62 Achsen tragen einen „Übergänge:"-Block als **Kommentar** (`beleg`, `job`, `upload`, `dispatch`), ein Dutzend weitere Pfeile im Kommentar — teils Übergänge, teils bloße Regeln. Z1-Tabellen gibt es für **zwei** Achsen (`zyklus_stapel` in `docs/topics/datev.md` R19, `produktbefund` in `docs/operations/produktbefunde.md`), und die einzige Tabelle im Code, `CASE_DOCUMENT_NUMBER_MODE_TRANSITIONS`, ist vollvermascht und hat eine andere Form | Neben den Descriptor-Maps ein `STATE_MACHINES: Partial<Record<StatusAxis, { description: string; transitions: StateTransition[] }>>`; der Deckungstest prüft, dass jedes `from`/`to` ein Schlüssel der Achse ist. Die vier Kommentar-Blöcke und die zwei Tabellen lassen sich sofort übertragen — danach entstehen die Z1-Tabellen aus den Daten statt von Hand (V13) — **erledigt 2026-09-07 (App-Seite): Commit `ba5da563`.** `STATE_MACHINES` trägt die Übergänge als Daten, mit Auslöser und Akteur; 0069 liest sie und nimmt eine Konstante nur noch für Achsen ohne Maschine | 0069 Befund 1 |
| **L-75** | **Z1 verlangt eine Tabelle je Achse, es gibt zwei.** `StateMachine` macht die Lücke sichtbar: jede Achse ohne Übergänge zeigt die Hinweiszeile. Das ist gewollt (R16), aber es sind rund fünfzig offene Posten | je Achse eine Tabelle, sobald L-74 die Datenform hat | zwei Tabellen in der Doku, Prosa in den Kommentaren; `StateMachine` zeigt die Reihe mit Hinweis | 0069 Befund 2 |
| **L-204** | **Drei JSONB-Spalten der Rechnungsposition sind im Anzeige-Typ `unknown`:** `vatEvidence` (Belegstellen, 40 % nicht leer), `vatNotes` (68 %) und `collapseDecisionJson` (die 9 Aggregat-Zeilen) in `InvoiceLineItem` | Struktur festschreiben und typisieren (`modules/invoices/domain/invoice.ts`); das Kollaps-Schema existiert bereits als Python `CollapseDecision` | Eine Komponente kann `unknown` nicht darstellen, und eine eigene Struktur dafür wäre die lokale Erfindung, die `spec-schreiben` §5 verbietet. Spec 0114 nimmt die drei deshalb als aufbereitete Props entgegen (`notes`, `collapse`) — die Darstellung ist frei, die Struktur nicht | `entitaeten/invoice-line.md` B7, Spec 0114 |
| ~~**L-205**~~ | **`ExpectationRow` trägt `resolvedAt` nicht.** Die Spalte `resolved_at` gibt es, `expectation-core.ts` schreibt und liest sie (Z. 178, 224, 432) — das gehobene Modell hat sie nicht. 0025 zeigt daran, ob die Erwartung erledigt ist; ohne das Feld bleibt es das einzige der Zeile, das nicht aus dem Spiegel kommt | `resolvedAt: string \| null` in `domain/expectation.ts` ergänzen | 0025 führt `resolvedAt` als einziges Feld seines Sichtmodells lokal | **Erledigt 2026-09-08, App-Commit `566693d5`.** `resolvedAt: string \| null` an `ExpectationRow`. Anmerkung der App: beide heutigen Leser filtern `resolved_at is null` und sehen dort immer `null` — das Feld ist für Sichten, die **erledigte** Erwartungen zeigen. Für 0025 heißt das: die Zeile kann den Zeitpunkt tragen, sobald eine solche Sicht existiert. |
| **L-206** | **Vier Felder des DATEV-Abzugs sind nicht mitgezogen.** `snapshot.ts` trägt Stichtag, Jahr, Import und Stufe; `contents`, `counts`, `reconcile` (die fünf Abgleich-Zähler) und `createdBy` stehen weiter in `snapshot-history-queries.ts`. Die Karte von 0027 zeigt alle vier | in `datev-mirror/domain/snapshot.ts` nachziehen (Muster L-04) | 0027 nimmt die vier Felder als eigene Ergänzung auf `Partial<DatevSnapshot>` | 0027, beim Typtausch 2026-09-07 |
| **L-207** | **`SourceDocumentVM` fehlen `caseNumber` und `hasInvoiceRow`.** Beides zeigt die Belegzeile: der Sachverhalt als Rang 6, und `hasInvoiceRow` entscheidet zusammen mit der Liste die Achse `beleg_haenger`. Der Anzeige-Typ trägt seit `62a2d0fa` alles andere | beide Felder in `source-document-vm.ts` aufnehmen | die Belegzeile bekommt beide vom Aufrufer | 0074, beim Typtausch 2026-09-07 |
| ~~**L-208**~~ | **`SourceDocumentDetail` bildet nur die Rechnung ab.** Der gespiegelte Typ hat vier Felder (`invoiceNumber`, `totalAmount`, `currency`, `taxAmount`); das Set führt eine Union über die Belegarten, die zusätzlich `net`, `vat`, `processingStatus` und die Felder des Vertrags trägt — das ist es, was 0076 zeichnet. Der reichere Typ bleibt deshalb vorerst lokal | die Union heben statt ihrer Rechnungs-Hälfte. **Zielform, bestätigt 2026-09-08:** `SourceDocumentDetail` wird eine unterschiedene Vereinigung über `kind`, `kind: "invoice"` mit `number`, `gross`, `currency: Currency \| null`, `processingStatus`, `net`, `vat`, `dueDate`, `paymentTerm`, `servicePeriod`, `paidAt`, `issuerVatId`, `originalCurrency`, `originalGross`; `kind: "contract"` mit `subject`, `amount`, `currency`, `contractType`, `startDate`, `endDate`, `durationMonths`, `isOpenEnded`, `bookingFacts: readonly ContractBookingFact[]`. Der Wortlaut steht in `src/ui/v3/entities/source-document/source-document-detail.ts` und wandert unverändert nach `source-docs/domain/`; das Set löscht seine Kopie beim nächsten Spiegel | **Erledigt 2026-09-08, App-Commit `bf9d8cfc`.** `SourceDocumentDetail` steht in `source-docs/domain/source-document-vm.ts` als Vereinigung über `kind`, im Wortlaut des Sets. Das Set löscht seine Kopie beim nächsten Spiegel. Eine Stelle beim Spiegeln beachten: `ContractBookingFact` kommt drüben über den Barrel `@/modules/contracts` (ESLint verbietet Deep-Imports zwischen Modulen) | 0074, 0076, beim Typtausch 2026-09-07 |
| ~~**L-209**~~ | **Zum `ludwigOnlyAmount` fehlt die Anzahl.** `AccountFactsVM` trägt den Betrag der Sätze ohne `datev_mirror_entry_id`, aber nicht ihre Zahl; `ludwigEntryCount` ist etwas anderes (alle Ludwig-Sätze des Jahres). 0066 schreibt den Satz mit der Anzahl — mit dem falschen Feld stünde dort eine größere, falsche Zahl | `ludwigOnlyCount: number` neben den Betrag | 0066 führt `ludwigOnlyCount` lokal neben dem gespiegelten Betrag | **Erledigt 2026-09-08, App-Commit `566693d5`.** `ludwigOnlyCount: number \| null` an `AccountFactsVM`. Die App sagt dazu: der Rechner fehlt noch, der Typ steht — heute füllt keine Aufrufstelle ihn (`ludwigOnlyAmount` ebenso wenig). Für 0066 heißt das: die Zahl darf gezeigt werden, wo sie da ist, und fällt weg, wo sie `null` ist. |
| **L-210** | **`ExpectationKind` ist zweimal definiert** — in `domain/case.ts` aus `EXPECTATION_KINDS` und seit `3048389d` noch einmal als Literal-Union in `domain/expectation.ts`. Zeichengleich, aber zwei Quellen: der App fällt es nicht auf, weil ihr Barrel namentlich exportiert; der Spiegel nutzt `export *` und wird mehrdeutig (TS2308). `scripts/mirror-barrels.mjs` löst es zugunsten von `case.ts` auf und meldet es bei jedem Sync | eine der beiden entfernen, sinnvoll die in `expectation.ts` | der Spiegel-Barrel entscheidet sich für `case.ts` und meldet die Dublette | Spiegel, 2026-09-07 |
| ~~**L-211**~~ | **`CaseDetail` trägt die verknüpften Belegnummern nicht.** Die Herabstufung des Belegnummern-Modus auf `single` verlangt nach `case.ts:102–107` „zusätzlich die Wahl der künftig gültigen Nummer" — dafür braucht der Editor die Liste der heute verknüpften Nummern. Der gespiegelte Detail-Typ führt sie nicht; der Aufrufer müsste sie aus den Belegen zusammensuchen und damit eine Ableitung im UI bauen, die die Domäne kennt | `documentNumbers: string[]` an `CaseDetail` (oder eine Ableitung neben `isDocumentNumberModeDowngrade`) | `CaseDocumentNumberModeEdit` (0083) nimmt sie als Prop vom Aufrufer; ohne sie ist die Herabstufung nicht wählbar | **Erledigt 2026-09-08, App-Commit `26a6718e` — anders als vorgeschlagen, und besser.** Nicht als Feld am `CaseDetail`, sondern als `listCaseDocumentNumberCandidates(caseId)` im Kern: das Register kostet acht Abfragen, und die Wahl erscheint nur bei der Herabstufung. Die Prop-Form von 0083 (`documentNumbers` vom Aufrufer) bleibt damit richtig — die App liefert sie jetzt über eine Server Action. |
| ~~**L-212**~~ | **`CaseDetail.disposition` ist `string` statt `CaseDisposition`.** Die Achse hat drei Werte und eine Schreib-Liste mit zweien (`CASE_DISPOSITION_WRITABLE`), beide im selben Modul — der Detail-Typ gibt trotzdem einen freien String heraus. Damit prüft nichts, dass ein Wert der Achse angehört, und ein Tippfehler fällt erst am Etikett auf (Präzedenz: `disposition: "kanzlei"` stand zwei Runden in einer Story, ohne dass ein Typ es merkte) | `disposition: CaseDisposition \| null` | `CaseDispositionEdit` (0083) engt selbst auf `"agent" \| "accounting"` ein; die Leseseite bleibt ungeprüft, bis der Typ steht | **Erledigt 2026-09-08, App-Commit `566693d5`.** `CaseDetail.disposition` ist `CaseDisposition \| null`. Damit fällt der Cast an jeder Aufrufstelle weg, und `CaseDispositionEdit` (0083) liest den Wert ohne Zusicherung. |
| ~~**L-213**~~ | ~~„Als geprüft bestätigen" hat am Vertrag kein Ziel.~~ **Erledigt 2026-09-08 durch Wegfall der Anforderung:** der Owner hat die Handlung gestrichen — weder Spalte noch Audit-Ereignis. 0073 endet beim Bearbeiten, und eine Prop, die nichts tut, wird nicht gebaut | — | — | Schema-Prüfung 0073, 2026-09-07; Entscheid 2026-09-08 |
| **L-214** | **`field_provenance` und `booking_facts` sind im Schema nicht beschrieben.** Beide sind `jsonb` ohne Spaltenkommentar; ihre Struktur steht nur im gespiegelten TypeScript (`ContractFieldProvenance`, `ContractBookingFact`) | je ein Spaltenkommentar mit der Form, wie ihn die Nachbartabellen tragen | wer das Schema allein liest, sieht zwei formlose Objekte; für das Set genügt der Spiegel | Schema-Prüfung 0073, 2026-09-07 |
| ~~**L-215**~~ | **`SourceDocumentVM.completedVia` ist `string` statt der Achse.** `SOURCE_DOC_COMPLETION_VIA` hat sechs Werte (`booking`, `case_closed`, `import`, `superseded`, `manual`, `no_booking_required`) und liegt im selben Modul wie das Anzeigemodell; der Typ gibt trotzdem einen freien String heraus. Der Grund der Erledigung ist genau das, was die Belegzeile im Tooltip zeigt — ein Tippfehler fällt erst am Etikett auf. Derselbe Fall wie L-212 | `completedVia: SourceDocCompletionVia \| null` | **Erledigt 2026-09-08, App-Commit `bf9d8cfc`.** `completedVia: SourceDocCompletionVia | null`, die Mapper reichen den Grund durch | 0070/0074, Vertragsfrage 2026-09-08 |
| ~~**L-216**~~ | **Der Teilbeleg hat keinen Platz für seinen Seitenbereich, also nimmt er den Dateinamen.** `SourceDocumentVM` führt weder `splitPageRange` noch `parentSourceDocId`; die Belegseite setzt deshalb „Seiten 5–7" als `fileName` und reicht das Eingangsdatum des Originals durch. Das Entitätsprofil führt beide Felder mit Rang (12 bzw. Relation „Sammel-Original", je 20 % gefüllt) und verlangt für die Liste der Teilbelege „1–5 **+ Seitenbereich**" — der Seitenbereich ist eine eigene Angabe, kein Dateiname | `splitPageRange: string \| null` und `parentSourceDocId: string \| null` in `source-document-vm.ts`. `receivedDate` und `fileName` bleiben **Pflicht**: die Spalte ist `NOT NULL`, und der Teilbeleg trägt die Datei seines Originals — das ist wahr und für die Spur nützlich. Kein `kind: "part"`: Teilbeleg ist eine **Beziehung** (`parent_source_doc_id`), keine Belegart, und eine Belegart mehr würde jede Stelle anfassen, die über Belegarten entscheidet | **Erledigt 2026-09-08, App-Commit `ae0e1a63`.** `splitPageRange` und `parentSourceDocId` stehen in `source-document-vm.ts`; `sourceDocumentFromDispatch` füllt sie | Vertragsfrage ludwig-manager, 2026-09-08 |
| ~~**L-217**~~ | **Drei Punkte der Beleg-Fakten stehen im Profil, aber nicht im Anzeigemodell.** `classOverriddenAt` (Rang 14, 3 % — Spaltenkommentar „UI: is not null → Hinweis") und die DATEV-Ablage `datevRefSystem`/`datevRefFolder`/`datevRefId` (Rang 16, 65 %) fehlen in `SourceDocumentVM`; `classConfidence` (Rang 13) ist da. Die Belegseite zeigt alle drei heute als eigene `FieldList`, weil die Form sie nicht bekommen kann | die vier Felder in `source-document-vm.ts` aufnehmen und in den Mappern füllen | **Erledigt 2026-09-08, App-Commit `ae0e1a63`.** `classOverriddenAt` und die drei `datevRef*` stehen im Anzeigemodell. **Offen bleibt eine Hälfte:** nur `sourceDocumentFromDispatch` füllt sie, die Listen-Mapper nicht (ihre Queries holen die vier Spalten nicht). Für 0120 reicht das — die Punkte gehören der Detailform, nicht der Liste | 0120, Vertragsfrage ludwig-manager 2026-09-08 |
| **L-219** | **`RowAction` kennt kein `ask`.** Seit 0121 kann eine Handlung vor dem Ausführen etwas erfragen — aber nur als Knopf (`ActionButton`) und als Sammelaktion (`BulkAction`). Eine **Zeilen**aktion kann es nicht, obwohl sie denselben Fall hat: die Lieferantenwahl bei „Einzeln", wenn der Resolver keinen Lieferanten kennt. Die App hat sie deshalb als dritte **Sammel**aktion gebaut („Einzeln, mit Lieferant") — eine Handlung an der falschen Stelle, weil die Schnittstelle die richtige nicht anbietet | `RowAction` bekommt `ask` wie `BulkAction`; das ist Aufgabe **0122** in diesem Repo, kein Befund für die App | `banks/offen` löst es über eine Sammelaktion | Rückmeldung ludwig-manager nach dem Bau von 0121, 2026-09-08 |
| ~~**L-218**~~ | **Die Achse `bank_match_stage` hat kein Wort für „nicht gelaufen".** Zwölf Werte, keiner für `NULL` — und `NULL` heißt etwas anderes als jeder von ihnen: die Kaskade ist nicht gelaufen, nicht „kein Kandidat". Also schreibt jeder Baustein das Wort selbst, und genau das ist passiert: `bank-transaction-columns.tsx` sagte „nicht gelaufen", `BankTransactionFacts.tsx` „Kaskade nicht gelaufen" — zwei Texte für denselben Zustand, das Muster, an dem ein Vokabular auseinanderfällt (Präzedenz L-67, `ProvenanceSource`) | einen Wert `not_run` in `BANK_MATCH_STAGE` aufnehmen, `kind: "info"`; dann trägt ihn `StatusBadge` wie die anderen elf | **Erledigt 2026-09-08, App-Commit `c1e8e752`.** `not_run` steht in der Achse, `bankMatchStage()` normalisiert `null`, `unclear_none` bleibt davon getrennt. Beim nächsten Spiegellauf fallen die zwei handgeschriebenen Texte im Set weg | 0101, Abnahme 2026-09-08 |
| ~~**L-220**~~ | **`overview-vm.ts` ist nicht spiegelbar.** Die Datei liegt in `domain/`, importiert aber `@/ludwig/modules/entries/client` und `../infrastructure/case-detail-queries` — der Spiegel-Filter hat sie beim Lauf vom 2026-09-08 ausgelassen (`scripts/mirror-filter.mjs` entscheidet am Import, nicht am Text). Damit kommt **`CaseHeaderVM.disposition`** hier nicht an, obwohl es drüben steht | die zwei Importe lösen — die Typen dorthin ziehen, wo sie hingehören, oder das VM in eine importfreie Datei schneiden. Vorbild: `status-registry.ts`, seit `cf681257` importfrei und genau deshalb spiegelbar | **Erledigt 2026-09-08, App-Commit `988d3dc5`.** Die Datei trägt nur noch Formen; die Regeln sind nach `application/case-overview.ts` gezogen, der `/client`-Pfad ist weg. Beim nächsten Lauf kommt `CaseHeaderVM.disposition` mit | Spiegellauf 2026-09-08 |
| ~~**L-221**~~ | **Die Bridge-Störung hat schon eine Achse — sie wird nur nicht benutzt.** `UebersichtTab.tsx:415` zeigt `<Badge tone="danger">Bridge-Störung</Badge>`, also ein nacktes Abzeichen ohne Wort für den eigentlichen Zustand. Dabei trägt die Meldung ihn: `faultReportRequest.status` ist `datevApiStatus | "flow_error"` (`modules/bridge/contract.ts:245`), und `datevApiStatus` **ist** die Registry-Achse `bridge_datev` mit ihren fünf Werten. Statt „Bridge-Störung" könnte dort „DATEV nicht erreichbar" stehen, mit dem Erklärtext „Netz, VPN, URL und Port prüfen" — genau der Unterschied, den der Registry-Kommentar selbst als teuersten Fehlschluss der Skala beschreibt (`no_clients` stand am 31.07.2026 stundenlang als „läuft" im Dashboard) | **Weder ein fünfter Wert in `abgleich_lauf` noch eine neue Achse.** `abgleich_lauf` bleibt, wie er ist — eine Störung ist kein Lauf-Zustand, da hat ludwig-manager recht. Aber die Achse dafür existiert: `StatusBadge axis="bridge_datev" status={fault.status}`. Ihr fehlt genau **ein** Wert, `flow_error` — der einzige `status`, den der Vertrag zulässt und die Achse nicht führt. Zielform: `flow_error: { label: "Ablauf gescheitert", kind: "danger", description: "Die Bridge hat den Vorgang begonnen und ihn nicht zu Ende gebracht — kein DATEV-Problem, sondern eines im Ablauf selbst." }`. Der `scope` (`datev_api`/`client_list`/`onboarding`/`snapshot`/`export`) ist **keine** zweite Achse, sondern der Ort — er gehört in den Satz daneben, nicht in ein Abzeichen | **Erledigt 2026-09-08, App-Commit `b9f3707a`.** `flow_error` steht in der Achse, `UebersichtTab` zeigt sie statt eines nackten Abzeichens, der `scope` als Text daneben. Bemerkenswert: der Deckungsgleichheits-Test prüft jetzt die **ganze** Vertragsunion — damit fällt ein künftiger sechster Status auf, bevor er als nacktes Badge endet | Seitenprofil `datev-spiegel.md`, Befund 3 vom 2026-09-08 |
| ~~**L-222**~~ | **`TYPICAL_NATURE` kennt vier von sechs Werten.** Die Union führt `goods`, `expense`, `mixed`, `unknown`; der DB-CHECK erlaubt zusätzlich `service` und `investment`, und im Bestand tragen **924 Partner** genau diese beiden (`service` 877, `investment` 47) — 47 % aller Partner mit einem Wert ungleich `unknown`. `PARTNER_NATURE_LABEL` hat für sie kein Wort; die Spalte „Art" der Partnerliste bleibt dort leer. Fehler im laufenden UI, derselbe Fall wie L-203. **Warum der Compiler schweigt:** nicht die Label-Map ist unvollständig, sondern die **Aufzählung** — `TYPICAL_NATURE` führt die vier, also ist `Record<TypicalNature, string>` vollzählig und `PARTNER_NATURE_LABEL[p.typicalNature]` typrichtig. Der Typ ist enger als der CHECK, und genau deshalb fällt die Lücke nirgends auf, bis eine Zelle leer bleibt | beide Werte in `TYPICAL_NATURE` **und** `PARTNER_NATURE_LABEL` aufnehmen (`modules/business-partners/domain/business-partner.ts`). Das deutsche Wort für `service` steht schon im GLOSSARY-Eintrag „VAT nature" (`Dienstleistung`), für `investment` fehlt eines | die Partner-Spaltendefinition zeigt einen Wert ohne Wort roh an, damit die Lücke sichtbar bleibt statt still zu verschwinden (Präzedenz 0072) **Erledigt 2026-09-08, App-Commit `a38169f4`.** `TYPICAL_NATURE` führt alle sechs Werte, die zweite Aufzählung in `create-partner-core.ts` ist weg, und ein Test liest den DB-CHECK **aus der Migration** — damit fällt eine künftige Erweiterung des CHECK auf, statt still eine Zelle zu leeren | `entitaeten/business-partner.md`, Rang 11 |
| **L-224** | **Keine Anzeigename-Ableitung für den Geschäftspartner.** `short_name` ist zu **98 %** gefüllt und in **49 %** ein anderer Text als `legal_name` (DATEV-Kurzbezeichnung, auf 15 Zeichen begrenzt) — trotzdem rendert jede Stelle `legalName` roh und hängt `shortName` dekorativ daran. `configuration/stammdaten` beschreibt den Kurznamen zugleich als Bestandteil des Buchungstexts | `partnerDisplayName({ legalName, shortName })` neben die Typen in `modules/business-partners/domain/business-partner.ts`, analog `documentCounterparty()` (L-36) und `caseDisplayTitle()` (L-52) | `BusinessPartnerCell` und die Spaltendefinition nehmen beide Felder getrennt entgegen und zeigen den Kurznamen als zweite Zeile; welcher führt, ist offene Frage 1 des Profils | `entitaeten/business-partner.md`, Rang 8 |
| **L-227** | **`BusinessPartnerListItem` trägt weder `mergedIntoPartnerId` noch `datevIsVariousAccount`.** `searchCreditors` filtert beide in SQL heraus — ein Picker, der den gespiegelten Typ benutzt, kann Grabsteine und Diverse-Konten nicht ausschließen, obwohl der Spaltenkommentar es verlangt („Grabsteine erscheinen in keiner Auswahl-Liste mehr"). Dazu ein zweiter Fund: `datev_is_various_account` ist in **0 von 14.890** Zeilen gesetzt, obwohl es laut GLOSSARY die **einzige** Wahrheit für Sammelkonten ist | beide Felder an `BusinessPartnerListItem`; getrennt davon klären, ob der Diverse-Import das Kennzeichen überhaupt schreibt | `BusinessPartnerPicker` bekommt seine Grundgesamtheit vom Aufrufer und filtert selbst nichts | `entitaeten/business-partner.md`, Rang 17 und der Satz „Ausgelassen — im Bestand leer oder tot" |
| **L-240** | **Zwei Anzeige-Typen der Wiederkehr-Regel liegen außerhalb des Spiegels.** `OverdueRecurringItem` steht in `accounting-cases/application/agent-reads-core.ts` (Z. 4278–4295), `RuleOverviewItem` in `recurring-rules/infrastructure/rule-overview-queries.ts` (Z. 25–51) — beides `application`/`infrastructure`, also nichts, was `sync-ludwig.sh` mitnimmt. Es sind genau die Zeilen-Modelle der beiden Regel-Listen | beide nach `recurring-rules/domain/` (Muster L-06/L-93) | `RecurringRuleRow` nimmt seine Felder einzeln als Props und definiert kein eigenes Zeilenmodell — ein eigenes wäre die lokale Erfindung, die `spec-schreiben` §5 verbietet | `entitaeten/recurring-rule.md` B1 |
| **L-255** | **Die Buchungssatz-Vorschau der Wiederkehr-Regel liefert fertige Beschriftungen statt Konten.** `buildRulePreview()` (`recurring-rules/domain/booking-preview.ts`) gibt `PreviewPosting { debit, credit, amount, taxKey }` zurück — `debit` und `credit` sind Strings aus `formatAccountLabel()`: „4200 Miete", und beim Rückfall „Bank (aus Zahlung)" bzw. „Personenkonto" **ganz ohne Nummer**. Die Lesform des Buchungssatzes im Set ist `JournalEntryCard`, deren `@when` die Regel-Vorschau namentlich nennt; sie braucht `JournalLine { side, accountNumber, accountName?, amount }`. Aus dem einen das andere zu machen hieße, einen Anzeigetext am ersten Leerzeichen zu zerschneiden und zu hoffen, dass davor eine Kontonummer stand — beim Rückfall geht es gar nicht. Dazu ist `PreviewPosting.amount` nullable, `JournalLine.amount` nicht | `RuleBookingPreview` trägt die Konten **strukturiert** (Nummer, Name, Seite, Betrag); `formatAccountLabel()` bleibt für die Hinweiszeile und die Rückfall-Wörter. Muster L-06: das Anzeigemodell ist Struktur, das Formatieren gehört der Sicht | `RecurringRuleFacts` (Spec 0134) nimmt die Vorschau als `{ lines: JournalLine[]; automatic; note }` — der **Aufrufer** übersetzt, und die Form zerlegt keinen Beschriftungs-String (§5) | Spec 0134, 2026-09-08 |
| **L-257** | **Kein gespiegelter Typ für das Zahlungskonto.** `client_payment_accounts` ist Elternteil der Wiederkehr-Regel (`payment_account_id`) und steht im Regel-Formular als Auswahl — `src/ludwig/` führt dafür keinen Typ, nur Streufelder anderer Module (`paymentAccountName` in `accounting-cases/domain/case-event.ts`, `paymentAccountKind` in `datev-export/domain/types.ts`). Ein Zahlungskonto ist auch kein Sachkonto: `AccountCandidate`/`AccountField` passen nicht | ein `PaymentAccount` (Id, Bezeichnung, IBAN, Bank) in der Domäne des Moduls, das die Konten führt — Muster L-05/L-14 | `RecurringRuleEditor` (Spec 0135) nimmt `readonly { id: string; label: string }[]` — die kleinste Struktur, die eine Auswahl braucht, und bewusst nicht mehr | Spec 0135, 2026-09-08 |
| ~~**L-261**~~ | **Der Schreibpfad kannte vier Felder nicht, die die Domäne führt.** `RecurringRuleDraft` (0135) leitet seine Felder aus `RecurringRule` ab; vier davon — `matchAmountTolerancePercent`, `matchContractNumber`, `matchDocumentTextRegex`, `matchingNote` — nahm die Save-Action der App nicht entgegen. **Kein Mangel des Sets:** alle vier stehen im gespiegelten Domänentyp (`recurring-rules/domain/rule.ts` Z. 226, 244, 250, 252), der Editor deckt also die Domäne ab, während die Schreibseite hinterherhing. Bemerkenswert, weil es die Richtung umkehrt, die diese Woche sonst galt: hier war nicht die Spec hinter dem Code, sondern der Schreibpfad hinter dem Datenmodell | **Die App erweitert den Save-Kern** (ein Kern pro Write, Entscheid ludwig-manager 2026-09-08). Bis dahin hält drüben `DRAFT_FIELDS_NOT_SAVED` plus ein Rundlauf-Schutz | im Set nichts — 0135 bleibt, wie er ist; die vier Felder sind keine Erfindung, sondern der Domänentyp **Erledigt 2026-09-08, App-Commit `75ba0647`.** Das Zod-Schema der Save-Action kannte drei Felder nicht, die `RuleWriteInput` seit F94/F108 führt; `matchingNote` läuft über `updateRuleMatchingNote` aus dem Save-Kern. `DRAFT_FIELDS_NOT_SAVED` ist weg | Rückmeldung ludwig-manager nach dem Anschluss von 0135, 2026-09-08 |
| ~~**L-262**~~ | **Zwei Ableitungen für denselben erwarteten Betrag.** `RuleOverviewItem.previewAmount` (`recurring-rules/infrastructure/rule-overview-queries.ts`) rechnet `matchAmount ?? Summe(template_lines)`; die Domäne rechnet `accrualAmount()` (`domain/rule.ts`) dreistufig als `template.amount → Summe(lines) → matchAmount`. Die Query wählt `template_amount` **gar nicht** aus, obwohl `rule-queries.ts` es tut (Z. 42, 148) — und `accrualAmount` nennt `matchAmount` ausdrücklich nur noch „Fallback für Altregeln, die vor der Trennung angelegt wurden" (F40 Teil C). Im Bestand unsichtbar, weil in allen 30 Zeilen beide Felder gleich sind; sobald eines abweicht, zeigt die Verwaltungssicht eine andere Zahl als der Regelwerk-Reiter desselben Falls. **Zu tun:** `template_amount` mitselektieren und `previewAmount` aus `accrualAmount()` bilden — eine Ableitung, nicht zwei | die Liste `RecurringRuleOverview` (0131) rechnet nicht: `amount` kommt fertig herein, und die Spec schreibt fest, dass es `accrualAmount()` sein **muss** **Erledigt 2026-09-08, App-Commit `4828f6c0`.** Die Query liefert Rohfelder, `accrualAmount()` rechnet — **eine** Wahrheit für die Zahl | Spec 0131, 2026-09-08 |
| ~~**L-263**~~ | **„Welche Regel greift doppelt?" ist aus einer Zeile nicht beantwortbar.** Der Job J-54 nennt den Doppelgriff als eine von drei Auffälligkeiten (Seitenprofil `regelwerk-mandant.md`), und im Bestand trägt genau **ein** Sachverhalt zwei Regeln (L-245). `listRulesOverviewForClient` stellt die beiden nur nebeneinander (`order by … case_number asc, priority asc`); eine Zahl „wie viele Regeln hängen an diesem Fall" gibt es nicht. Aus den ausgelieferten Zeilen zu zählen ist falsch, sobald ein Filter greift — dann kann die zweite Regel ausgeblendet sein und die Marke behauptet etwas Unwahres. **Zu tun:** `count(*) over (partition by r.case_id)` als `case_rule_count` in `RuleOverviewItem` | `RecurringRuleRow`/der Spaltensatz nimmt `caseRuleCount` je Zeile und setzt die Marke „2 Regeln" nur, wenn die Zahl da ist; ohne sie bleibt die dritte Auffälligkeit unsichtbar **Erledigt 2026-09-08, App-Commit `4828f6c0`.** `count(*) over (partition by case_id)` liefert `caseRuleCount`; im Bestand zeigt sie den einen Doppelgriff (`2026-0116`) | Spec 0131, 2026-09-08 |
| ~~**L-264**~~ | **Die Verwaltungssicht wählt drei Felder nicht aus, die ihre eigene Spaltenliste braucht.** `listRulesOverviewForClient` selektiert weder `document_number_strategy` (Rang 22, im Bestand **100 %** gefüllt: `period_key` 26 · `from_document` 3 · `fixed` 1) noch `profile_source` (Rang 23: `onboarding` 24 · `derived` 5) noch `matches_documents`. Gerade die drei sind der Grund für die Sicht: `fixed` bricht ab der zweiten Periode den OPOS-Ausgleich (GLOSSARY `Belegnummern-Strategie`), und 24 von 30 Regeln tragen ein `onboarding`-Profil, das nie ein Mensch geprüft hat (L-252). **Zu tun:** drei Spalten in den `select` und in `RuleOverviewItem` | `RecurringRuleOverview` (0131) führt die Spalte „Belegnummern-Strategie" im benannten Satz; sie bleibt leer, bis die Query sie liefert. `profileSource` steht im Ausbau der Spec **Erledigt 2026-09-08, App-Commit `4828f6c0`.** Die drei Spalten werden selektiert | Spec 0131, 2026-09-08 |

## B · GLOSSARY

| # | Befund | Zu tun | Quelle |
|---|---|---|---|
| **L-18** | Kein Eintrag für das **Ereignis** (`client_accounting_event`) — es kommt nur in der Schichten-Tabelle und im Sachverhalts-Eintrag vor | Eintrag anlegen | 0040 |
| **L-19** | Kein Eintrag für die **Rohdaten-Sicht**; „raw" ist bisher nur als Gegenstück zu Stammdaten belegt (`vendor` vs. `creditor`) | Eintrag anlegen | 0051 |
| **L-20** | Kein Eintrag für den **Kontoauszug**, und drei Wörter im Umlauf: „Kontenblatt", „Konto-Auszug", „Konto-Historie" (`AccountLedgerDrawer`, `getAccountLedger`, `listAccountMirrorEntries`, Fuß-Text „Volles Konto öffnen") | **ein** Wort wählen, dann Eintrag | `entitaeten/account.md` |
| **L-55** | **Zwei deutsche Namen für `client_bank_transactions`, beide im GLOSSARY:** der Eintrag `### Bank transaction` sagt „Banktransaktion", die Schichten-Tabelle desselben Dokuments sagt „Kontoauszugsposition". Die App sagt im UI durchgehend „Kontoauszug" (`KontoauszugView`, `kontoauszug-presentation`, Route `banks`). Zweite Ausprägung von L-20 | **ein** Wort wählen, dann den Eintrag nachziehen. Das Profil folgt bis dahin der Schichten-Tabelle und dem UI: **Kontoauszugsposition**; der englische Name `bank transaction` ist unstrittig | `entitaeten/bank-transaction.md` B1 |
| **L-65** | Der Eintrag **Accounting case** lässt in seiner `lifecycle_status`-Aufzählung `waiting_for_documents` aus, obwohl der DB-CHECK ihn führt und die Liste einen eigenen Reiter „Wartet auf Unterlagen" dafür hat | Aufzählung ergänzen | `entitaeten/accounting-case.md`, Prüflauf 2026-09-05 |
| **L-21** | „Ledger account status" nennt `'active' \| 'archived'`, der DB-CHECK erlaubt `'active' \| 'inactive'`. Die Registry folgt der DB — der GLOSSARY-Satz („`archived` blendet Konten in der Buchungsmaske aus") hat **keinen Wert hinter sich** | GLOSSARY korrigieren oder die Spalte erweitern | `entitaeten/account.md` |
| **L-72** | Die drei Stufen von `baseline_level` haben **keine Wörter**: weder im GLOSSARY noch als Label in der Domäne. `SnapshotCard` zeigt bis dahin den rohen Wert in `MonoCell` — keine lokale Map | Wörter ins GLOSSARY, Label in die Domäne. Dazu **L-04 präzisieren**: `datev-mirror` und `datev-truth` haben kein `domain/`, der Spiegel nimmt aus beiden Modulen nichts — **halb erledigt 2026-09-07 (App-Seite): Commit `222c8d5a`.** Die Wörter stehen als `BASELINE_LEVEL_LABEL` in `datev-mirror/domain/snapshot.ts`, und 0027 zeigt sie statt des Rohwerts. **Offen bleibt der GLOSSARY-Eintrag** — `GLOSSARY.md` trägt drüben fremde Änderungen | 0027, Freigabe 2026-09-06 |
| **L-76** | Kein Eintrag für **„Übergang / transition"** — Z1 spricht von „Hinein durch / Hinaus durch", der Code von `transition` | Eintrag „Transition (Übergang)" mit Verweis auf Z1 und die Datenform aus L-74 | 0069 Befund 3 |
| **L-77** | Kein Eintrag für **„Gegenkonto (contra account)"**, obwohl der Buchungssatz-Editor damit arbeitet und die Props danach benannt werden | Eintrag anlegen | 0015, Freigabe 2026-09-06 |
| **L-78** | **Tailwinds Preflight lädt nach den eigenen Stylesheets** und setzt `a { color: inherit; text-decoration: inherit }`. Bei gleicher Spezifität gewinnt die spätere Regel — damit sieht **jeder** Link aus wie gewöhnlicher Text, unterscheidbar erst beim Zeigen. Gemessen im Set vor dem Notbehelf: `rgb(45,45,45)` statt `rgb(43,111,156)`. Die App lädt dieselbe Kette in derselben Reihenfolge (`index.css` sagt es im Kopf) | `@tailwind base` **vor** die eigenen Stylesheets ziehen — das ist die Ordnung, die Tailwind vorsieht. Der Zug ändert in einem Schritt auch Überschriften, Listen und Knöpfe, braucht also einen Durchgang durchs Storybook bzw. durch die Seiten | 0111 |
| **L-79** | **Zurückgezogen und ersetzt** (Abnahme 0070, 2026-09-07). Die Behauptung „beides steht am Eingang, keins im View-Model" stimmte nicht: `InboxEntry` trägt `classConfidence: number \| null` und `byteSize: number \| null`, `StoredFile` trägt `byteSize` — genau die zwei Listen, die die Spalten brauchen (Inbox, Einreichen), lesen `InboxEntry`. Was bleibt, ist das **Durchreichen**: das View-Model der Belegliste führt beide Felder nicht, obwohl die Quelle sie hat | `classConfidence` und `sizeBytes` aus `InboxEntry`/`ops_stored_files` ins View-Model der Belegliste durchreichen — nicht neu erheben | 0070, Abnahme 2026-09-07 |
| **L-80** | **Die Konfidenz einer Einordnung hat keine Achse.** `class_confidence` ist `numeric(5,4)`, also ein Anteil zwischen 0 und 1. Die Achse `konfidenz`, die dem Namen nach passt, gehört dem **Buchungsvorschlag** (`client_journal_entry.proposal_confidence`) und antwortet auf eine Belegklassifikation mit „Bitte Konto und Steuerschlüssel prüfen". Bis eine eigene Achse steht, zeigt die Spalte den Anteil als Prozentzahl — so, wie die Belegtabs der App ihn schon schreiben | Achse `beleg_konfidenz` nach dem Z-Verfahren anlegen (gebandet, mit Beschreibungen), oder festhalten, dass die Konfidenz eine Zahl bleibt und keine Achse bekommt | 0070, Abnahme 2026-09-07 |
| **L-81** | **Die Ableitung der Hänger-Achse steht in der UI.** `StuckDocumentsTable` rechnet die vier Werte von `beleg_haenger` inline aus `hasInvoiceRow` und der Ausprägung. Eine Ableitung, die das UI braucht, gehört in `domain/` — sonst steht sie in jeder Oberfläche neu, hier wie dort | `stuckState(hasInvoiceRow, variant)` in `modules/source-docs/domain/` legen; das Set liest sie dann von dort statt sie zu spiegeln | 0070, Abnahme 2026-09-07 |
| **L-82** | **`ProcessingProgress` hängt an der Rechnungszeile, nicht am Beleg.** Ein Vertrag oder ein Kontoauszug, der in der Extraktion steckt, zeigt auf der Detailseite keinen Fortschritt — nur einen leeren Fakten-Block. Die Achse `beleg` hängt am Beleg, der Fortschritt gehört an dieselbe Stelle | Fortschritt aus dem Basis-Beleg speisen (`processing_status` + `processing_stage` der Basiszeile), nicht aus `client_source_docs_invoices` | 0071, Seitenprofil `beleg-detail.md` |
| **L-83** | **`deferred_reason`, `deferred_count` und `deferred_by_clarification_id` fehlen im Anzeige-Typ der Klärung.** Beide Spalten gibt es; ohne den Zähler kann die Karte nicht vor der Sperre warnen („ab der dritten Verschiebung nur noch ein Mensch"), ohne den Verweis wird aus „endet mit der Antwort auf «…»" ein nacktes Datum | Alle drei Felder ins View-Model der Klärung aufnehmen — der Grund ist in der Datenbank Pflicht, ohne ihn ist eine Verschiebung nicht zurückzulesen | 0065 |
| **L-84** | **Das Buchungsdatum steht im Kontoauszug ohne Jahr** (`fmtDateShort` → „15.04."). Die Liste ist über Jahresgrenzen filterbar; ein Datum ohne Jahr ist dort mehrdeutig, und es ist zugleich das **Buchungs**-, nicht das Wertstellungsdatum (siehe L-61) | Das Jahr mitschreiben — `Time format="date" length="short"` im Set tut es bereits | 0085, Seitenprofil `kontoauszug.md` |
| **L-85** | **Der Kopf des Kontoauszugs nennt weder Zeitraum noch Saldo.** Wer den Auszug gegen den Papierauszug hält, sucht beides zuerst. Der Saldo hängt an L-58 (die Zahlen fehlen), der **Zeitraum** nicht — er steht in der Abfrage und wird nur nicht angezeigt | Zeitraum in die Kopfzeile der Seite; den Saldo, sobald L-58 steht, in den Fuß der Liste (nie in die Zeile) | 0085, Seitenprofil `kontoauszug.md` |
| **L-86** | **Der Kontenplan hat nur einen Leerfall.** „Keine Konten gefunden — passe die Filter an" steht auch dann da, wenn der Mandant im Jahr gar keinen Kontenrahmen hat. Zwei verschiedene Lagen, ein Satz — und der falsche schickt die Rolle in einen Filter, der nicht das Problem ist (V9) | Zwei Sätze: „Kein Konto in diesem Wirtschaftsjahr" (Bestand) ≠ „Keine Treffer" (Filter, mit Weg zurück) | 0062, Seitenprofil `kontenplan.md` |
| **L-87** | **Die gruppierte Kontenansicht hat keinen Pager.** `AccountsGroupedTable` wird ohne `Pagination` gerendert; bei 41.570 Konten je Mandant und Jahr gruppiert sie damit den **Bestand** statt der Seite | Die Gruppierung auf die geladene Seite anwenden und den Pager auch dort zeigen — dieselbe Liste, eine Anzeigeform mehr | 0062, Seitenprofil `kontenplan.md` |
| ~~**L-88**~~ | **Die Kontoseite lädt zwei Auszüge für eine Frage.** „Buchungen in DATEV" und „Buchungen in Ludwig" stehen als getrennte Tabellen mit je eigener Suche und eigenem Pager; das Entitätsprofil hat die Trennung am 2026-09-04 verworfen (die Frage ist eine, die Quelle ist eine Eigenschaft der Zeile). Erst vereinigt wird der Abgleich je Zeile (Achse `mirror_match`) sichtbar — der eigentliche Grund, warum jemand die Seite öffnet | **Erledigt 2026-09-08, App-Commit `230cfa6c`.** `loadAccountEntries` vereinigt beide Quellen — eine Suche, ein Pager, eine Sortierung, `origin` als Wert an der Zeile. Im Speicher zusammengeführt und auf Konto + Jahr beschnitten; der Weg zu einer UNION-Abfrage steht als `ponytail:`-Notiz in der Datei | 0063, Seitenprofil `konto-detail.md` |
| ~~**L-89**~~ | **`AccountRow` trägt keinen Geschäftspartner.** Rang 8 des Entitätsprofils, 52 % Füllgrad (auf Personenkonten ~100 %) — die Zeile hat `businessPartnerId` nicht und den Namen erst recht nicht. Der Spaltensatz nimmt ihn deshalb als Callback vom Aufrufer entgegen und lässt die Spalte sonst leer, statt etwas zu erfinden | `businessPartnerId` und den Anzeigenamen in `AccountRow` aufnehmen (die Relation gibt es, `AccountsTab` im Partner zeigt sie schon von der anderen Seite) — **erledigt 2026-09-07 (App-Seite): Commit `0f3c4358`.** `AccountRow` trägt `businessPartnerId` und `businessPartnerName`; die Partner-Spalte von 0062 liest sie, der Callback ist zur Überschreibung geworden | 0062 |
| ~~**L-90**~~ | **`AccountFilter.usedOnly` verspricht „mindestens eine Buchung" und filtert über `status`.** Die Bedingung des Profils lautet `usage_booking_count > 0`; die Abfrage nimmt `status = 'active'`. Das sind zwei verschiedene Mengen: 99 % der Konten sind `active`, aber 85 % haben null Buchungen — die Voreinstellung filtert also praktisch nichts weg. Dazu passt die Registry nicht: sie beschreibt `konto.inactive` als „angelegt, nie bebucht", was die Daten nicht hergeben | `usedOnly` auf `usage_booking_count > 0` umstellen **oder** den Registry-Text und den Filternamen an das anpassen, was er tut. Beides zugleich stehen zu lassen ist die teure Variante: die Kanzlei sieht 41.570 Zeilen und glaubt, sie sähe 6.212 — **erledigt 2026-09-07 (App-Seite): Commit `0f3c4358`.** `usedOnly` filtert über `usage_booking_count > 0` | 0062, Seitenprofil `kontenplan.md` |
| ~~**L-91**~~ | **`DEFERRAL_MAX_DAYS` fehlt in `modules/accounting-cases/domain/case.ts`.** Die Wiedervorlage darf höchstens 30 Tage vorausgehen (F105, ein Monatslauf) — die Zahl steht in keiner Domänenkonstante, also spiegelt das Set sie und schreibt sie einmal in die Karte. Dasselbe gilt für eine etwaige Mindestlänge des Grundes: im Spiegel ist kein CHECK zu finden, die Spec hat die Behauptung deshalb gestrichen | `DEFERRAL_MAX_DAYS = 30` (und, falls es sie gibt, die Mindestlänge) in `domain/case.ts` legen; das Set liest sie dann von dort — **erledigt 2026-09-07 (App-Seite): Commit `9c60caf4`.** `MAX_DEFERRAL_DAYS` steht in `accounting-cases/domain/case.ts`; 0065 importiert die 30, statt sie zu wiederholen | 0065 |
| **L-92** | **`belegTabLabel()` widerspricht dem Entscheid „ein Name für alle".** Der erste Reiter der Belegseite heißt je nach Belegart „Buchung", „Vertrag" oder „Beleg" — und zeigt in allen drei Fällen dasselbe: das Original. Was sich unterscheidet, ist die Box daneben. Eine Aufschrift, die wechselt, während der Inhalt gleich bleibt, ist eine falsche Fährte. Bei vier statt sechs Reitern sind zusätzlich `DOC_TABS` und `parseDocTab` nachzuziehen (der alte Slug `buchung` wird dort schon umgeleitet, das Muster steht also) | `belegTabLabel()` streichen, der Reiter heißt „Beleg"; `DOC_TABS` auf vier kürzen, `pipeline` und `rohdaten` als Tiefe im Verlauf — **halb erledigt 2026-09-07 (App-Seite).** Die wechselnde Aufschrift ist weg; `DOC_TABS` fällt mit der Seite in Welle 1 | 0071, Seitenprofil `beleg-detail.md` |
| ~~**L-93**~~ | **`SourceDocumentVM` hat kein Gegenstück im Spiegel.** Der Anzeige-Typ der ganzen Beleg-Familie ist v3-lokal definiert (`SourceDocument.tsx`); im Register stand bisher nur das **Durchreichen** einzelner Felder (L-79), nicht der Typ. Solange er hier wohnt, kann die App ihn nicht als Vertrag lesen, und jede Abweichung fällt erst beim Einbau auf | Einen Anzeige-Typ für den Beleg in `modules/source-docs/domain/` legen und über `sync:ludwig` spiegeln — dieselbe Bewegung wie bei `InboxEntry` — **erledigt 2026-09-07 (App-Seite): Commit `62a2d0fa`.** `SourceDocumentVM` liegt in `source-docs/domain/source-document-vm.ts`; die Familie setzt darauf auf und trägt nur noch Routen und zwei fehlende Felder selbst (L-207, L-208) | 0071 |
| ~~**L-94**~~ | **`AccountFactsVM` trägt im Spiegel weder `datevBalance` noch `ludwigOnlyAmount` noch `currency`.** Die Kontoseite beantwortet Rang 2 („wie viel liegt drauf") mit genau diesen drei Werten — DATEV führend, Ludwig als Delta —, und der Typ, aus dem sie kommen sollen, ist v3-lokal. L-13 ist damit nur halb erledigt: die Fakten stehen, die Zahlen darin nicht | Die drei Felder in den Anzeige-Typ des Kontos aufnehmen; dazu Σ Soll und Σ Haben (Rang 7 des Profils), die heute nirgends stehen — **erledigt 2026-09-07 (App-Seite): Commit `eaf73d45`.** `AccountFactsVM` trägt `currency`, `totalDebit`, `totalCredit`, `datevBalance` und `ludwigOnlyAmount`; `accountFacts()` summiert Σ Soll/Haben aus den Monatswerten. 0066 und 0063 lesen von dort | 0063 |
| **L-95** | **Zwei von drei Punkten der Kontoseite hatten keinen Spiegel-Typ** — beide sind erledigt: Σ Soll/Σ Haben je Konto und Jahr und die Monatswerte des Verlaufs (`{ month, debit, credit }`) stehen seit 2026-09-07 als `AccountFactsVM` und `AccountMonth` in `accounts/domain/account-entry.ts` (**Commit `eaf73d45`**), 0066 und 0063 lesen sie von dort. **Der dritte Punkt war falsch beschrieben und ist hiermit berichtigt** (2026-09-07, im Auftrag des Owners, designsystem-f0): Kontenfunktion und Automatik-Steuersatz stehen **nicht** inline in `accounts/[accountNumber]/page.tsx` — die Seite zeigt sie überhaupt nicht. Es gibt sie als Spalten (`datev_main_function`, `datev_main_function_number`, `datev_tax_rate` auf `client_ledger_accounts`) und als Vokabular (`core/datev/main-function.ts`, geprüft in `core/datev/automatic-account.ts`); `AccountRow` und `AccountFactsVM` tragen sie nicht. Damit ist dieses Drittel **kein Fundort-Befund**, sondern der Wunsch des Entitätsprofils, zwei vorhandene Spalten in den Typ zu heben | offen: `datevTaxRate` und die Kontenfunktion in `AccountFactsVM` aufnehmen, wenn eine Ansicht sie zeigt | 0063, Seitenprofil `konto-detail.md` |
| ~~**L-96**~~ | **`AccountRow.origin` hat keine Label-Map.** Die Zeile unterscheidet „im Mandanten angelegt" von „nur im SKR-Katalog" — das ist die ganze Frage der Katalogansicht —, aber im Gegensatz zu `source` (`ACCOUNT_SOURCE_LABEL`) gibt es dafür keine Wörter in der Domäne. Das Set setzt sie deshalb einmal als Vorgabewert und lässt den Aufrufer sie überschreiben, statt sie an zwei Stellen zu erfinden | `ACCOUNT_ORIGIN_LABEL` in `modules/accounts/domain/account.ts` anlegen (die App sagt dieselbe Sache heute als Badge „SKR-Katalog") — **erledigt 2026-09-07 (App-Seite): Commit `eaf73d45`.** `ACCOUNT_ORIGIN_LABEL` steht in der Domäne; 0062 nimmt es als Vorgabe, die Prop bleibt Überschreibung | 0062, Abnahme 2026-09-07 |
| **L-98** | **Kein GLOSSARY-Eintrag für die Rechnungsposition selbst**, und die drei Einträge *über* ihre Felder (`Invoice line item source`, `Fund usage nature`, `Accounting subject`) nennen eine Tabelle, die es nicht gibt: `client_invoice_line_items`. Die Tabelle heißt `client_source_docs_invoice_lines`. Wer nach dem GLOSSARY sucht, findet nichts — und der englische Name der Entität ist nirgends festgelegt | Eintrag „Invoice line (Rechnungsposition)" anlegen, die drei bestehenden auf den richtigen Tabellennamen ziehen | `entitaeten/invoice-line.md` |
| **L-99** | **Vier Wertebereiche der Rechnungsposition ohne Registry-Achse.** `source` (3 Werte), `fund_usage_nature` (6), `line_special_type` (12), `vat_special_case` (11) haben deutsche Wörter nur im GLOSSARY-Fließtext oder gar nicht. Eine Zeile kann sie damit nicht als Zustand zeigen, ohne eine lokale Label-Map zu bauen — und die verbietet R1. Die fünf Nachträge, die hier hingen, haben eigene Nummern bekommen: **L-200 bis L-204** | Je Wertebereich entscheiden: Registry-Achse (mit Beschreibung je Wert) oder Label-Map in `modules/invoices/domain/`. `line_special_type` und `vat_special_case` sind zu ~75 % `none` — für sie genügt vielleicht ein Wort für die Ausnahme | `entitaeten/invoice-line.md` B2 |
| **L-203** | **Fehler im laufenden UI: die Label-Maps in `PositionenTab` treffen die echten Werte nicht.** `VAT_SPECIAL_CASE_LABEL` kennt acht Schlüssel, von denen im Bestand genau **einer** vorkommt (`reverse_charge_non_eu`, 24 Zeilen) — die anderen **92** Zeilen mit Sonderfall zeigen dem Nutzer den englischen Rohschlüssel (`exempt_other`, `small_business_exemption`, `reverse_charge_eu_service` …). `LINE_SPECIAL_TYPE_LABEL` fehlen `summary_total` (40) und `summary_tax` (2). Das ist keine Datenlücke, sondern falscher Text auf dem Bildschirm — und es zeigt, warum L-99 eilt: wer die Map in die neue Zeile kopiert, kopiert den Fehler mit | Maps gegen die tatsächlich vorkommenden Werte prüfen und vervollständigen — besser: mit L-99 durch Achsen ersetzen. Die neue Zeile (Spec 0072) nimmt die Wörter bis dahin als Prop und zeigt einen Wert ohne Wort roh an, damit ein fehlender Eintrag sichtbar bleibt statt still zu verschwinden | `entitaeten/invoice-line.md` B6, Prüfung 2026-09-07 |
| **L-223** | **`VAT_PROFILE_LABEL` lebt privat in `MasterDataTab.tsx`.** Neun Werte, deutsche Wörter weder im GLOSSARY-Eintrag „Creditor VAT profile" noch in der Domäne — die **Partnerliste zeigt deshalb den rohen Enum** (`domestic_reverse_charge` statt eines Wortes), in einer Spalte, die unter den tatsächlich benutzten Partnern zu 73 % gefüllt ist. Fehler im laufenden UI, dritte Ausprägung von L-99/L-203 | Die neun deutschen Wörter in den GLOSSARY-Eintrag, dann `VAT_PROFILE_LABEL` aus `MasterDataTab.tsx` nach `modules/business-partners/domain/business-partner.ts` heben und exportieren. **Keine Registry-Achse:** das USt-Profil ist eine Eigenschaft, kein Zustand — kein Fortschritt, keine Farbe; dieselbe Begründung, mit der `PARTNER_NATURE_LABEL` dort schon nicht steht | `entitaeten/business-partner.md`, Rang 10 |
| **L-225** | **Der GLOSSARY-Eintrag „Creditor source" ist F76 nicht gefolgt.** Er heißt nach der Rolle statt nach der Entität (die Spalte ist `client_business_partners.source`) und zählt **sechs** von acht CHECK-Werten auf — `opos_import` und `recurring_import` fehlen, obwohl `create-partner-core.ts` sie schreibt. Eine Label-Map gibt es zu keinem der acht; `MasterDataTab` und `TechnicalTab` zeigen die Herkunft roh | Eintrag umbenennen („Business partner source / Partnerherkunft"), die zwei Werte ergänzen, dann `PARTNER_SOURCE_LABEL` in `modules/business-partners/domain/` | `entitaeten/business-partner.md`, Rang 16 |
| **L-241** | **`is_active` der Wiederkehr-Regel wird farbig gezeigt, hat aber keine Achse.** `RegelwerkTab.tsx` Z. 180–186 baut `<Badge tone="success" dot>aktiv</Badge>` / `<Badge tone="neutral">inaktiv</Badge>` von Hand. Die Spalte ist zu 100 % gefüllt (29 aktiv · 1 inaktiv von 30) und ist neben dem Buchungsmodus der zweite Zustand der Regel: eine inaktive Regel ordnet nichts zu und stellt nichts soll. Derselbe Fall wie L-53 bei `CaseKindCell` | Entweder eine Registry-Achse mit Beschreibung je Wert, oder die Farbe weg. `valid_until` ist **kein** Ersatz — 0 % gefüllt und laut GLOSSARY „rein informativ". *Hier wartet darauf:* `RecurringRuleRow` zeigt die Gültigkeit als Wort ohne Farbe, bis das entschieden ist | `entitaeten/recurring-rule.md` B2 |
| ~~**L-242**~~ | **`document_number_strategy` und `profile_source` haben keine deutschen Wörter im Code** — keine Registry-Achse, keine Label-Map, kein `*_LABEL`. `document_number_strategy` ist zu **100 %** gefüllt (`period_key` 26 · `from_document` 3 · `fixed` 1 von 30) und steht in `stapelabnahme/ui/Schritt3Wiederkehrend` als „Belegfeld-Strategie" **roh englisch** auf dem Bildschirm; `profile_source` (97 %) wird nirgends gezeigt, obwohl der GLOSSARY sagt: „Ohne die Herkunft wäre eine falsche Ableitung später nicht auffindbar." Gleiche Bauform wie L-99 | Je Wertebereich entscheiden: Registry-Achse (mit Beschreibung je Wert) oder Label-Map in `recurring-rules/domain/`. Der GLOSSARY beschreibt beide Begriffe schon, aber je Wert gibt es kein Wort. *Hier wartet darauf:* `RecurringRuleFacts` und die Regel-Listen nehmen die Wörter als Prop und zeigen einen Wert ohne Wort roh an, damit die Lücke sichtbar bleibt statt still zu verschwinden **Erledigt 2026-09-08, App-Commit `f0a0bd00`.** Die Wortlisten stehen in der Domäne neben `RULE_INTERVAL_LABEL` | `entitaeten/recurring-rule.md` B3 |
| **L-246** | **Kein GLOSSARY-Eintrag für den Dauersachverhalt-Kandidaten** (`RecurringCandidate`, `recurring-rules/domain/derive-recurring-candidates.ts`). Er hat eine Registry-Achse (`dauersachverhalt_uebernahme`, vier Werte), eine eigene Review-Oberfläche und drei Klassen — aber keinen Eintrag; der Begriff steht nur in den Notes von `Recurring rule` und `LDSV mit WK` | Eintrag anlegen: englisch `recurring candidate`, deutsch `Dauersachverhalt-Kandidat`, **keine Tabelle** (on-read aus `ops_datev_ingest_staging`, keine Persistenz), mit den drei Klassen und dem Pseudowert `bereits_angelegt`. *Hier wartet darauf:* Aufgabe 0130 (Kandidaten-Liste) und die Frage, ob der Kandidat ein eigenes Entitätsprofil bekommt | `entitaeten/recurring-rule.md` B7 |
| **L-247** | **„recurring" heißt dreierlei, und der GLOSSARY benennt die Kollision nicht.** (a) `client_source_docs_invoices.billing_mode = 'recurring'` — die **Dauerrechnung**, eine Aussage über den Beleg. (b) `client_accounting_case.kind = 'recurring_charge'` — der **Dauersachverhalt** (LDSV), 105 Fälle im Bestand. (c) `client_accounting_case_rule` — die **Wiederkehr-Regel**, 30 Zeilen. Alle drei haben Einträge, keiner nennt die beiden anderen als Abgrenzung. Dazu ein falscher Verweis: `Billing mode` schreibt „`recurring` eröffnet einen **Dauersachverhalt** ([[Recurring rule]], `create_recurring_case`)" — der Link zeigt auf die Regel, der Satz meint den Sachverhalt | Je Eintrag ein Abgrenzungssatz („nicht zu verwechseln mit …"; Muster: `Belegnummern-Strategie` hat ihn gegen den `Belegnummern-Modus` schon), und den Verweis in `Billing mode` auf `[[Accounting case]]` richten. *Hier wartet darauf:* nichts — das Entitätsprofil trägt die Abgrenzung als eigene Tabelle im Kopf, damit keine Spec sie neu herleitet | `entitaeten/recurring-rule.md` B8 |
| ~~**L-256**~~ | **`RuleDirection` hat keine deutschen Wörter im Code.** Die Union (`payment_in` / `payment_out`) trägt die Richtung der Wiederkehr-Regel — 93 % gefüllt (26 · 2 · 2 × NULL) — und bestimmt Soll und Haben (`proposalSides`, `accrualSides`). Wörter gibt es nur **innerhalb** von `describeRecurringRule()` als Inline-Strings (`dirWord` in `rule-summary.ts`: „Zahlungseingang", „Zahlungsausgang", und für NULL „passenden Umsatz") — keine `*_LABEL`-Konstante, kein Export. Die Registry-Achse `ereignis_art` führt dieselben zwei Schlüssel mit denselben Wörtern, ist aber `client_accounting_event.kind`: die Achse des **Ereignisses** an ein Feld der **Regel** zu hängen wäre derselbe Fehler wie die falsche Map in L-243, nur andersherum. Gleiche Bauform wie L-99 und L-242 | `RULE_DIRECTION_LABEL` neben `RULE_INTERVAL_LABEL` in `recurring-rules/domain/rule.ts` — dort steht das Muster schon („reines Vokabular ohne Farbe"); der dritte Fall (NULL = „passenden Umsatz") gehört dazu. *Hier wartet darauf:* `RecurringRuleRow` (0132) und `RecurringRuleFacts` (0134) nehmen die Wörter als Prop und zeigen einen Wert ohne Wort roh an, damit die Lücke sichtbar bleibt statt still zu verschwinden **Erledigt 2026-09-08, App-Commit `f0a0bd00`.** `RuleDirection` hat deutsche Wörter im Code — dieselbe Änderung wie L-242, drei Listen an einem Ort | Spec 0132, 2026-09-08 |

## C · Schema und Datenbestand

| # | Befund | Zu tun | Quelle |
|---|---|---|---|
| **L-229** | **Auf die Partner-Stammdaten führt kein Schreibpfad mehr.** `update_business_partner` ist mit F127 als Agenten-Werkzeug gestrichen, eine Server Action gibt es nicht. Für das Design-System ist das die Begründung gegen einen `BusinessPartnerEditor` — sie wird dadurch stärker. Fachlich ist es eine **Owner-Frage**: ein falscher Ort oder eine falsche USt-IdNr. ist heute nur über einen erneuten Import zu korrigieren | entscheiden, ob das so bleiben soll; wenn nicht, den Schreibpfad benennen | `entitaeten/business-partner.md`, Prüfung 2026-09-09 |
| **L-22** | `client_ledger_accounts.is_default` ist **tot**: 41.570 von 41.570 Zeilen `false` | befüllen oder droppen; die Anzeige lässt sie aus | `entitaeten/account.md` |
| **L-23** | `client_accounting_case_expectation` hat `expected_amount`, aber **keine `currency`** | Spalte ergänzen; bis dahin nimmt der Aufrufer die des Sachverhalts | 0040 |
| **L-24** | `client_accounting_case_expectation.clarification_id` ist **tot**: 0 von 45 Zeilen belegt, seit F125 auch fachlich abgelöst | Kandidat zum Entfernen | clarification B4 |
| **L-25** | **Kein `created_by` an der Klärung.** Wer gefragt hat, steht nur im Audit (`case.clarification_raised`, 403 Ereignisse, 3 mit User-Actor); die Tabelle kennt `answered_by`, aber keinen Fragesteller. **Der Owner will Erstellung und Beantwortung mit Person und Datum sehen** | Spalte ergänzen **oder** den Audit-Verlauf in `ClarificationVM` laden | clarification B7 |
| **L-26** | `professional_text` und `client_text` sind in **79 %** identisch — die Zwei-Text-Regel wird faktisch nicht gelebt | entweder erzeugen die Module wirklich zwei Fassungen, oder die Regel wird auf eine reduziert. Die UI kann beides tragen, aber nicht beides gleichzeitig behaupten | clarification B3 |
| **L-27** | **Eine Antwort je Frage, kein Faden.** `answerClarification` lehnt die zweite ab; ein zweiter Anlauf ist eine neue Zeile ohne Kante zur ersten | Datenmodell-Entscheidung (Owner-Frage 2026-09-04): n Antworten je Frage, oder ein Faden über `parent_clarification_id`. Die Karte trägt schon 0…n Einträge | clarification B8 |
| **L-28** | Altlast `document_missing` / `document_upload` — je 8 Zeilen, seit F125 fachlich abgeschafft | Migration oder bewusste Duldung; die neue Karte bietet `document_upload` nicht mehr als Antwortform an | clarification B6 |
| **L-29** | **Tag gegen Zeitstempel**: `event_date` und `due_date` sind `date`, `created_at` der Klärung ist `timestamptz` — der Verlauf ist tagesgenau, `Timeline` zeigt Uhrzeiten | entscheiden, welche Auflösung der Verlauf hat | 0040 |
| ~~**L-30**~~ | **Der DATEV-Auszug kennt keinen Saldo.** `listAccountMirrorEntries` summiert Soll und Haben je Satz, den laufenden Saldo rechnet die Kontoseite nur über die Ludwig-Sätze — führt der Spiegel die Liste an, führt er auch den Saldo | Saldo über die Spiegelsätze rechnen — **außer bei Sammelkonten** (Owner-Entscheid 2026-09-08): der Spiegel ist die Nebenbuch-Sicht mit gedroppten Sammelkonto-Legs, die Zahl wäre dort **falsch**, nicht bloß ungenau. Sammelkonten (an der Kontenfunktion erkennbar) zeigen statt einer Zahl den Satz „über den Spiegel nicht darstellbar (Sammelkonto)". Die App setzt es zusammen mit **L-88** im `accounts`-Paket um | **Erledigt 2026-09-08, App-Commit `230cfa6c`.** Der Spiegel-Saldo folgt derselben Regel wie der Ludwig-Saldo (aufsteigend übers Jahr, dann filtern); für Hauptbuch-Sammelkonten `null` plus `mirrorBalanceUnavailable`. Die Erkennung ist eine **Konstante je Kontenrahmen** (`accounts/domain/collective-accounts.ts`), kein Feld — siehe `entitaeten/account.md` | `entitaeten/account.md` |
| **L-35** | **Diskriminator und Subtyp-Zeile laufen auseinander** — 10 von 384, und zwar in **zwei** Richtungen: 7 × `invoice` ohne `…_invoices`-Zeile (alle 2026-07-31, vor dem F87-Kern, alle erledigt — Altbestand) und 3 × `other` **mit** Zeile, alle drei mit gesetztem `class_overridden_at` | Die drei sind App-seitig `P26` („Belegform-Override räumt die Rechnungs-Pipeline nicht auf"), Owner-Entscheid offen. Für uns nichts zu tun: die Familie zeigt den Ausprägungs-Block nur, wenn beide Quellen übereinstimmen, und deckt damit beide Richtungen ab (0074). Die Regel fällt weg, sobald P26 entschieden ist | `entitaeten/source-document.md` B9 |
| ~~**L-38**~~ | ~~Vier Belegarten ohne Subtyp-Tabelle~~ — **erledigt 2026-09-04 (App-Seite): Absicht, kein Befund.** Die drei sind Container bzw. Deckblätter; ihre Struktur liegt an `client_bank_import_batches` / `client_bank_transactions` bzw. an den Kind-Belegen. Der fehlende Satz steht seit 2026-09-04 als Punkt 4 im GLOSSARY-Eintrag „Source document supertype & specializations" | nichts zu tun. Für uns: der Deckblatt-Block hängt an der Gruppen-Relation, nicht an der Belegart (0076) | `entitaeten/source-document.md` B2, B12 |
| ~~**L-39**~~ | ~~`client_source_docs_contracts` ist leer~~ — **erledigt 2026-09-04 (App-Seite): kein Befund.** Alle sieben Audit-Ereignisse hängen an einem Beleg, den es nach einem Mandanten-Wipe (nach 2026-07-20) nicht mehr gibt; das Audit-Log ist Seitenkanal und überlebt das per Design. Kein Schreibpfad-Fehler | nichts zu tun. Für das Design-System heißt es: den Vertragsrenderer gegen das **Schema** bauen, nicht gegen Staging — die Tabelle war dort nie produktiv befüllt (0076) | `entitaeten/source-document.md` B1 |
| **L-40** | `doc_category` zu 46 % gefüllt — **beantwortet 2026-09-04 (App-Seite):** die F87-Migration ist bewusst backfill-frei. Seit 2026-08-20 fehlt die Kategorie nur noch bei `other`-Belegen (23 Stück), und das ist Absicht: **ein Container hat keine Kategorie, sie entsteht je Teilbeleg.** Die restlichen ~170 sind Staging-Altbestand Juli/August | Der Backfill ist App-seitig `P20` (Big-Bang-Reset, wartet auf Owner-Go), keine neue Migration. **Regel fürs Design-System, unabhängig davon:** ein Filter oder Badge über die Kategorie führt NULL als „unklassifiziert / Container", nie als Fehler oder Lücke | `entitaeten/source-document.md` B3 |
| ~~**L-41**~~ | Die Erledigung wird in **drei** Formen gezeigt — Häkchen in der Belegliste, Häkchen in `SourceDocFamily`, Badge mit Wort nur in `DocCompletionControl`. Begründet mit „binär, kein eigener Werteraum" (`COMPLETED_LEGEND`, von Hand). **Das stimmt nicht:** `completed_via` ist CHECK-beschränkt auf sechs Werte und zu 73 % gefüllt (`booking` 171 · `manual` 77 · `no_booking_required` 26 · `superseded` 7 · `case_closed` 1 · 61 erledigt ohne `via` · 41 offen) | Achse aufnehmen; v3 führt sie als `beleg_erledigung` (die sechs Werte plus „Offen"), `completed_reason` bleibt der Tooltip  **Seit 2026-09-05 blockiert L-41 die Aufgabe 0080:** die Achse ist die einzige, die die lokale Kopie der Status-Registry mehr führt als der Spiegel — solange sie drüben fehlt, kann die Kopie nicht gelöscht werden, ohne `SourceDocument.tsx` zu brechen.  — **erledigt 2026-09-05 (App-Seite): Commit `41a1a8d3`.** Der Block ist aus `3c0682f` übernommen, alle acht Schlüssel gleich; `open` und `completed` kommen als Extra-Werte dazu, weil sie Aussagen über `completed_at` sind und keine Werte der Spalte. Damit ist 0080 entblockt und abgeschlossen. | `entitaeten/source-document.md` B8 |
| **L-42** | Die Achse `beleg` (Pipeline) lebt am **Rechnungs**-Subtyp; für 16 % der Belege gibt es dort nie einen Wert. Die Supertyp-Achse `beleg_inbox` ist nur die halbe Antwort — 383 von 384 tragen denselben Wert (`classified`) | entscheiden, welcher Zustand einen Nicht-Rechnungs-Beleg unterscheidet. v3 stellt bis dahin die **Erledigung** nach vorn und `beleg_inbox` erst ab M | `entitaeten/source-document.md` B4 |
| **L-43** | `listInvoicesForClient` liest vom Supertyp und liefert alle Belegarten — der Name behauptet das Gegenteil und ist die häufigste Quelle des Missverständnisses „Beleg = Rechnung" | umbenennen (`listSourceDocsForClient`) | `entitaeten/source-document.md` B7 |
| **L-45** | **Die Kante Beleg → Bank-Umsätze ist weich** (von der App-Seite 2026-09-04 bestätigt und übernommen). Zwei Wege, kein FK: `client_bank_transactions.raw_payload->>'source_doc_id'` samt `external_transaction_id = '<source_doc_id>#<n>'` (Abtipp-Weg; im Bestand 79 Umsätze an 6 Belegen) und `client_bank_import_batches.stored_file_id = client_source_docs.stored_file_id` (Intake F133, 0 Zeilen). Der Batch trägt die Kopfdaten des Kontoauszugs, kennt den Beleg aber nicht | Vorschlag der App-Seite: `client_bank_import_batches.source_doc_id` als nullable FK, vom Abtipp- und Intake-Weg gesetzt — eine Spalte, kein Subtyp. **Owner-Entscheid steht aus.** Solange offen, zeigt keine Beleg-Form Zeitraum, Salden oder Zeilenzahl eines Kontoauszugs (0076 Ausbau) | `entitaeten/source-document.md` B11 |
| **L-46** | **Der Klammer-Typ wird nie vergeben** (App-Seite `P28`, 2026-09-04). 12 Sammel-Originale: 8 × `not_connected`, 4 × NULL, keine der vier Klammer-Familien aus belege.md R25–R27. Ursache im Klassifikator (die LLM-Antworten liefern `other`/`null`/`not_connected`, nie `credit_card_statement` oder `expense_report`), nicht in der Persistenz — der Verprobungspfad läuft leer | App-seitig erfasst als P28. **Für uns:** der Deckblatt-Block muss ohne Klammer-Typ vollständig aussehen — im Bestand ist das der Normalfall, nicht die Ausnahme (0076) | `entitaeten/source-document.md` B13 |
| **L-58** | **Der Kontoauszug zeigt keinen Saldo.** Die Kontoübersicht (`[year]/banks`) führt je Konto eine Saldo-Spalte, der Auszug selbst (`KontoauszugView`) keinen laufenden Saldo — wer gegen den Bankauszug abgleicht, hat keine Anschlusszahl | Anfangs- und Endsaldo des gefilterten Zeitraums unter der Liste (nicht je Zeile: eine Saldo-Spalte stimmt nur bei genau einer Sortierung). Schwesterbefund zu L-30 | `entitaeten/bank-transaction.md` B4 |
| **L-200** | **`accounting_type` hat keinen Konsumenten.** Die Spalte der Rechnungsposition ist zu **98 %** gefüllt und kommt nirgends an: kein GLOSSARY-Eintrag, kein Feld im Anzeige-Typ `InvoiceLineItem`, keine Verwendung im UI | klären, ob sie jemand vermisst oder ob sie tot ist — dann Eintrag und Feld, oder droppen | `entitaeten/invoice-line.md` B3 |
| **L-201** | **`line_discount_value` ist zu 0 % gefüllt** (726 von 726 leer) und hat trotzdem eine eigene Spalte in der heutigen Kopfzeile von `PositionenTab` („Rabatt", fünfte von sieben) — in jeder Zeile steht dort „—" | befüllen oder Spalte streichen. Die neue Zeile (Spec 0072) baut sie nicht; sie steht dort im Ausbau und kommt zurück, sobald ein Wert darin steht | `entitaeten/invoice-line.md` B4 |
| **L-202** | **Drei Anzeige-Zweige in `PositionenTab` laufen ins Leere, einer davon ist ein Fehler im laufenden UI.** (a) Die Tabelle „Alternative Kategorien (Historie)" rendert nie: `history_candidates_json` ist in **726 von 726** Zeilen ein leeres Array. (b) Die Klammer „19 % (extrahiert: 7 %)" rendert nie: `vat_extracted_rate_percent` ist in 559 Zeilen gesetzt und weicht in **null** davon von `tax_rate_percent` ab. (c) Die DATEV-Steuerschlüssel-Kandidaten hängen im Block „USt-Sonderbehandlung" und erscheinen nur mit einem USt-Sonderfall — **423 der 440** gefüllten Kandidatenlisten haben keinen und bleiben unsichtbar | (c) ist ein Fehler in der Bedingung und sofort behebbar: der Block gehört an die Kandidatenliste, nicht an den Sonderfall (Spec 0114 baut ihn so). (a) und (b) erst entscheiden — tote Spalten wie L-201 oder ein Interpreter, der sie nicht befüllt | `entitaeten/invoice-line.md` B5, Prüfung 2026-09-07 |
| **L-226** | **Die Historie eines Geschäftspartners liegt unter drei `resource_kind`-Werten.** `platform_audit_events` führt `business_partner` (39 Ereignisse: `account_assigned` 16, `linked_to_clearing_account` 16, `created_by_agent` 7), daneben `creditor` (33) und `debtor` (2) — seit F76 ist das **eine** Entität. Eine Verlaufsliste am Partner zeigt je nach Filter ein Drittel bis die Hälfte seiner Geschichte | Entscheiden: die beiden alten Werte auf `business_partner` migrieren, oder der Leser fragt alle drei ab. Solange das offen ist, bekommt der Partner keinen Verlauf — `0127` (`BusinessPartnerView`) führt den Befund als Voraussetzung | `entitaeten/business-partner.md`, Relationen-Tabelle |
| ~~**L-244**~~ | **`needsModeReview()` prüft ein Präfix, das es nicht gibt.** `recurring-rules/domain/booking-preview.ts` fragt `importReference.startsWith("datev-wkb")`; der F91-Import schreibt laut GLOSSARY und laut Bestand `datev-wk:<Belegnummer>` — **0 von 30** Zeilen beginnen mit `datev-wkb`, **29** mit `datev-wk:`. Die Warnung „Modus prüfen?" im `RegelwerkTab` (Z. 205) kann deshalb nie erscheinen **Und warum es niemandem auffiel:** `booking-preview.test.ts` prüft die Funktion mit `importReference: "datev-wkb:20260016"` — der Test benutzt dasselbe Präfix wie der Code und bestätigt damit eine Annahme, die der Import nie erfüllt. Ein grüner Test über einem Fall, den es nicht gibt; der JSDoc der Funktion schreibt `datev-wkb:*` gleich mit. — **erledigt 2026-09-08 (App-Seite): Commit `b7544542`.** Das Präfix heißt jetzt `datev-wk:` (mit Doppelpunkt, sonst nähme die Prüfung jedes Präfix mit, das so anfängt), Code und Test sind richtiggestellt. Die Warnung kann damit erscheinen — im Bestand trägt allerdings keine Regel gleichzeitig `book_on_payment` und ein Personenkonto | Präfix auf `datev-wk` korrigieren — und dann prüfen, ob die Heuristik noch trägt: im Bestand hat **keine** Regel gleichzeitig `book_on_payment` und ein Personenkonto. *Hier wartet darauf:* `RecurringRuleFacts` zeigt den Hinweis, sobald die Ableitung ihn liefert; das Set baut die Heuristik nicht nach **Erledigt 2026-09-08, App-Commit `b7544542`.** Das Präfix heißt `datev-wk:`, und der Test prüft jetzt den **Doppelpunkt** statt das falsche Präfix — der Test, der den Fehler zementierte, prüft jetzt das Richtige | `entitaeten/recurring-rule.md` B5 |
| **L-245** | **Die Oberfläche kennt nur EINE Regel je Sachverhalt.** Der Tabellenkommentar von `client_accounting_case_rule` sagt ausdrücklich „Mehrere Regeln pro Case erlaubt (kein Unique auf `case_id`)", und im Bestand trägt **ein** Sachverhalt zwei. `RegelwerkTab` nimmt `existingRule: RecurringRule \| null` — die zweite Regel ist unsichtbar, und `priority` (sie entscheidet, welche gewinnt; im Bestand steht sie in allen 30 Zeilen auf `100`, ist also wirkungslos) wird weder gezeigt noch bearbeitet | Entweder zeigt der Reiter n Regeln (dann braucht er das Zeilenmodell aus L-240), oder das Schema bekommt das Unique, das der Kommentar ausschließt. Eine Entscheidung ist nötig, weil beide Stände heute gleichzeitig behauptet werden. *Hier wartet darauf:* `RecurringRuleRow` ist so geschnitten, dass mehrere Regeln eines Falls untereinander stehen können | `entitaeten/recurring-rule.md` B6 |
| **L-248** | **Der Bestand kann die halbe Wiederkehr-Regel nicht bestätigen.** Neun Spalten sind zu 0 % gefüllt (`match_counterparty_iban`, `match_purpose_regex`, `match_contract_number`, `match_document_text_regex`, `template_lines`, `match_amount_tolerance_percent`, `matching_note`, `payment_account_id`, `valid_until`), und `match_amount_tolerance` steht in allen 30 Zeilen auf 0,00 — weil **jede** der 30 Regeln aus dem F91-Onboarding-Import stammt (29 × `import_reference` `datev-wk:`, angelegt 2026-08-21 bis 2026-09-07, drei Mandanten) und **keine** aus einer Zahlung gelernt wurde | Kein Fix, sondern eine Bitte: Regeln aus dem Agenten-Pfad (`prefillFromTransaction`) in Staging, sonst bleibt der halbe Wertebereich unbeobachtet. *Hier wartet darauf:* die Ränge 19–21 des Profils stützen sich ausdrücklich nicht auf den Füllgrad, sondern auf die Ableitung — die Prüfung des Profils muss das wissen | `entitaeten/recurring-rule.md` B9 |
| ~~**L-249**~~ | **`matches_documents` ist bei 29 von 30 Regeln `true`, obwohl keine ein Beleg-Kriterium trägt.** `match_contract_number` und `match_document_text_regex` sind zu 0 % gefüllt; getragen wird der Beleg-Treffer damit allein vom Personenkonto (`matchSourceDoc`). In der Sache heißt das „jeder Beleg dieses Kreditors gehört an diesen Dauersachverhalt" — der Spaltenkommentar verspricht dagegen „wird beim Setzen eines Beleg-Kriteriums abgeleitet". Dazu: die **ganze Belegseite** der Regel (F94) ist in keiner Oberfläche sichtbar, auch nicht im `ZuordnungTab`, der sonst alle Kriterien auflistet | Klären, ob die Ableitung so gemeint ist; wenn ja, gehört der Satz in den Spaltenkommentar und ins GLOSSARY. Unabhängig davon gehört die Belegseite in die Kriterien-Anzeige. *Hier wartet darauf:* `RecurringRuleFacts` zeigt die Belegseite als eigene Gruppe neben der Zahlungsseite (Rang 18) **Erledigt 2026-09-08, App-Commit `ba13b719` — und die Ursache war eine andere als hier stand.** Der Bestand war **stimmig**: die 29 `true` sind genau die 29 Regeln mit Personenkonto (F91-Regel), die eine ohne trägt `false`. Der Fehler saß im Schreibpfad — `updateRule` schrieb das Feld gar nicht, nur `insertRule`. Jetzt rechnet `matchesDocuments()` in der Domäne, beide Schreiber nutzen sie; kein Daten-Fix nötig. **Was daraus zu lernen ist:** dieser Befund las eine Verteilung als Widerspruch („97 % `true` ohne Beleg-Kriterium") und traf damit das Symptom, nicht die Sache — der Beleg-Treffer wird laut Domäne eben auch vom Personenkonto getragen. Eine auffällige Verteilung ist ein Anlass nachzusehen, noch keine Diagnose | `entitaeten/recurring-rule.md` B10 |
| **L-250** | **Die Wiederkehr-Regel hat keinen eigenen `resource_kind` in der Historie.** `platform_audit_events` führt **18 benannte** `resource_kind`-Werte (dazu 14 Ereignisse ohne Wert), `recurring_rule` ist keiner; die Regel-Ereignisse hängen am Fall (`case.rule_created_by_agent` 6, `case.rule_updated_by_agent` 3, `case.recurring_created_by_agent` 79, `onboarding.recurring_cases_created` 3). „Wer hat diese Regel wann geändert" ist an der Regel damit nicht beantwortbar — und die Regel ist das Objekt, dessen stille Änderung am teuersten ist, weil sie automatisch bucht | Entweder ein `resource_kind='recurring_rule'`, oder der Entscheid „Regel-Historie bleibt bewusst am Fall" gehört ins GLOSSARY. *Hier wartet darauf:* `RecurringRuleFacts` zeigt keine Historie; das Profil verweist für sie auf `accounting-case` | `entitaeten/recurring-rule.md` B11 |

## D · Doppelte Implementierungen in der App

Nicht dringend, aber jede Dopplung ist eine zweite Wahrheit, die bei der
Ablösung doppelt Arbeit macht.

| # | Befund | Quelle |
|---|---|---|
| **L-228** | `topVendors` wird je Seitenaufruf gerechnet und **nirgends gelesen**: ein Join über zwei Tabellen mit `limit 5` in `modules/clients/infrastructure/dashboard-queries.ts`, kein Treffer in `apps/web/src`. Die Kachel „Top Kreditoren" ist per Owner-Entscheid gestrichen (begründet im Kopfkommentar der Dashboard-Seite), die Abfrage blieb stehen | Abfrage entfernen, oder die Kachel wiederherstellen | `entitaeten/business-partner.md`, Prüfung 2026-09-09 |
| ~~**L-31**~~ | Die beiden `RohdatenTab.tsx` sind Kopien voneinander; die Sachverhalt-Fassung ist die ältere (keine Klappe, keine `HEAVY_TABLES`, `<pre>` fest bei 360 px). **Wer sie ablöst, löst zwei Dateien ab.** Dazu drei Wahrheiten für denselben Wert: `true` vs. „ja", Zahl gruppiert vs. ungruppiert, Zeitstempel lokalisiert vs. roh. — **erledigt 2026-09-06 (App-Seite): Commit `7e452218`.** die beiden Rohdaten-Tabs sind eine generische `ui/RawDataView.tsx` | 0051 |
| ~~**L-32**~~ | Zwei Antwort-Eingaben für dieselbe Frage: `AnswerInput` (Kanzlei) und die Kopie in `PortalCaseList.tsx`. `RueckfragenListe` nutzt bewusst dieselbe Eingabe wie der Sachverhalt — das Portal ist ausgeschert. — **erledigt 2026-09-06 (App-Seite): Commit `7e452218`.** das tote Portal-Modul samt `AnswerInput`-Kopie ist gelöscht | clarification B5 |
| ~~**L-33**~~ | Zwei Sicht-Mechaniken, eine Bedeutung: `InvoiceLogsPanel` (Fachlich/Technisch/Beide + verbose, Client-State) und das Stapel-Log (Verlauf/Protokoll/Technik, URL). Mit 0053/0054 wird „Fachlich" = Tiefe ≤ 2, „Technisch" = Tiefe 3, „verbose" = schwerefreie Tiefe-3-Zeilen — **der Schalter fällt weg**. — **erledigt 2026-09-06 (App-Seite): Commit `e8b5eb77`.** die Log-Familie läuft über `LogList`/`LogBrowser`, der verbose-Schalter ist weg, und Level → Tiefe liegt in der Domäne | 0054 |
| **L-34** | Acht Module bauen das Auf- und Zuklappen lokal nach: `AuditLogTable`, `KontoauszugView`, `FindingsList`, `Schritt6Liste`, `PositionenTab`, `GlanceCard`, `ClarificationsBanner`, `OpenExportOverview`. Beim Umzug auf `DataTable`s `expand` fällt der jeweilige `useState` weg | 0057 |
| **L-59** | Der **Kontoauszug verschluckt die SEPA-Referenzen**: 90 % der Zeilen tragen mindestens eine, `KontoauszugView` zeigt aber nur den Freitext — es benutzt `PurposeDisplay` gar nicht (der String `purpose` kommt in der Datei kein einziges Mal vor) und rendert `deriveSepa(row).text` als nacktes `<span>`. Das (i) der Zeile öffnet den Drawer, nicht den Zweck. Damit ist der Originalblock aus der Hauptliste nicht erreichbar | `entitaeten/bank-transaction.md`, Prüflauf 2026-09-05 |
| **L-60** | `reassignImportBatch` ist **toter Code**: in `konten.md` R21–R22 und `bank.md` als Korrekturweg dokumentiert, im ganzen Repo aber ohne Aufrufer — kein UI, keine Server Action, kein Agenten-Werkzeug, kein Skript. Entweder anschließen oder Dokumentation und Funktion streichen | `entitaeten/bank-transaction.md`, Prüflauf 2026-09-05 |
| **L-61** | `SachverhaltScreen.BankPane` beschriftet ein Feld „Wertstellung", füllt es aber aus `postingDate` (`overview-vm.bankFacts`). Valuta und Buchungsdatum sind zwei Spalten und weichen regelmäßig ab | `entitaeten/bank-transaction.md`, Prüflauf 2026-09-05 |
| **L-62** | Eine **dritte Route zeigt Kontoauszugspositionen**, die in keinem Profil stand: `configuration/bankkonten/[accountId]/transactions` rendert dieselbe Zuordnungstabelle über alle Zeilen eines Kontos und schneidet **stumm bei `limit 500`** ab — ohne Pager und ohne Hinweis, dass etwas fehlt | `entitaeten/bank-transaction.md`, Prüflauf 2026-09-05 |
| **L-63** | Der **Zuständigkeits-Filter der Sachverhaltsliste ist tot**: `CaseListFilters` schreibt `?dispo=` in die URL, `caseFilterForListTab()` liest ihn nicht, und `CaseFilter` hat kein Feld dafür. Ein sichtbarer Filter, der nichts filtert — der v3-Spaltensatz übernimmt ihn erst, wenn er wirkt | `entitaeten/accounting-case.md`, Prüflauf 2026-09-05 |
| **L-64** | `CaseOverviewBox` und `EventStack` sind exportiert, werden aber **nirgends gerendert**; `docs/ui-repraesentationen.md` §1 führt beide weiter als aktive Komponenten. Vor der Ablösung klären, ob sie tot sind — sonst zählt die Migration Arbeit, die es nicht gibt | `entitaeten/accounting-case.md`, Prüflauf 2026-09-05 |
| **L-243** *(halb)* | **Zwei Fehler im laufenden UI, beide dieselbe Ursache — (b) erledigt, (a) offen: lokale Wörter statt der vorhandenen Quelle.** (a) `stapelabnahme/ui/Schritt5.tsx` Z. 99 gibt `i.expectedInterval` direkt aus — auf dem Bildschirm steht `monthly`, obwohl `RULE_INTERVAL_LABEL` im selben Modul liegt und der `RegelwerkTab` sie benutzt. (b) `stapelabnahme/application/wiederkehrende.ts` Z. 77–88 hält zwei eigene Maps: `MODUS` bildet `fixed`/`variable`/`document_driven` ab — **keinen** der drei echten `booking_mode`-Werte —, sodass `MODUS[ruleMode]` immer danebengreift und der Rohwert `accrue_then_settle` erscheint (29 von 30 Regeln); `INTERVALL` führt zusätzlich `weekly`, das der DB-CHECK nicht erlaubt. Beide Maps sind Kopien dessen, was Registry (`regel_modus`) und Domäne (`RULE_INTERVAL_LABEL`) schon führen. **Zu tun:** die zwei lokalen Maps löschen, `resolveStatus("regel_modus", …)` und `RULE_INTERVAL_LABEL` benutzen **(b) erledigt 2026-09-08 (App-Seite): Commit `5e48d892`** (die zwei lokalen Maps in `stapelabnahme/application/wiederkehrende.ts` sind weg). **(a) steht offen:** `stapelabnahme/ui/Schritt5.tsx:100` gibt `i.expectedInterval` weiterhin roh aus — auf dem Bildschirm steht `monthly`, obwohl `RULE_INTERVAL_LABEL` exportiert danebenliegt. *Hier wartet darauf:* `RecurringRuleRow` (0132) und `RecurringRuleList` (0133) lösen genau diese Zeile ab | `entitaeten/recurring-rule.md` B4 |
| **L-251** | **Die Übernahme-Klasse des Dauersachverhalt-Kandidaten wird von Hand nachgebaut, obwohl die Achse danebensteht.** In `app/(app)/admin/tenants/[tenantId]/clients/[clientId]/page.tsx` Z. 884–893 stehen vier `Badge`-Zweige, während die **Kopfzeile derselben Tabelle** die Achse `dauersachverhalt_uebernahme` über `StatusHeader` schon benutzt (Z. 836–840). Sie sind bereits abgedriftet: die Achse kennt `bereits_angelegt`, die Zelle schreibt „bereits angelegt", und das `⚠` für `reviewFlag` steht in keiner Achse. Dazu zwei handgebaute `<div>`-Leerfälle statt `EmptyState`. **Zu tun:** `StatusBadge axis="dauersachverhalt_uebernahme"` statt der vier Zweige; `reviewFlag` als Wort daneben, nicht als Glyphe im Etikett | `entitaeten/recurring-rule.md` B12, Aufgabe 0130 |
| **L-252** | **Die Verwaltungssicht des Regelwerks ist gebaut und nie angeschlossen.** `recurring-rules/infrastructure/rule-overview-queries.ts` (205 Z.) liefert `listRulesOverviewForClient` → `RuleOverviewItem` mit 22 Feldern, mandantenweit über alle Sachverhalte, sortiert `is_active desc, case_number asc, priority asc`. **Kein Aufrufer**; der einzige Verweis außerhalb der Datei ist der Barrel-Export in `server.ts` Z. 19–20, und keine der 45 Routen unter `app/(app)/**` fragt Regeln sachverhaltsübergreifend ab. Damit fehlt der einzige Ort, an dem die zweite Regel eines Sachverhalts (L-245) und eine ungeprüfte `profile_source='onboarding'`-Ableitung (24 von 30) auffallen könnten. **Zu tun:** anschließen oder löschen — das Set legt die Liste als Aufgabe 0131 an, sie setzt auf `RecurringRuleRow` auf | `entitaeten/recurring-rule.md` B13, Aufgabe 0131 |
| ~~**L-253**~~ | **Zwei Funktionen zählen verschiedene Kriterien — und die Oberfläche glaubt der falschen.** `matchTransaction()` (`recurring-rules/domain/rule.ts:388`) setzt für `matchPurposeRegex` ausdrücklich `anyCriterion = true`; `hasAnyCriterion()` (`rule-summary.ts:25–31`) prüft nur Name, IBAN und Betrag. Eine Regel mit **nur** einem Zweck-Regex **greift** also, während `describeRecurringRule()` daneben schreibt „Diese Regel hat noch keine Match-Kriterien und greift daher bei keiner Zahlung." Ein Satz, der das Gegenteil dessen behauptet, was die Maschine tut — und das Entitätsprofil erhebt ihn zur Anzeige-Regel — **erledigt 2026-09-08 (App-Seite): Commit `2c0c888f`.** `MATCH_CRITERIA` führt jedes Kriterium einmal, mit beiden Fragen (`isSet`, `matches`); `matchTransaction()` prüft darüber, `hasAnyCriterion()` zählt darüber. `RuleSummaryInput.matchPurposeRegex` ist neu und **optional** — wer es beim Aufruf weglässt, bekommt für eine Regel mit nur einem Zweck-Regex weiter den falschen Satz; die Specs 0134/0135 machen es dem Aufrufer deshalb zur Pflicht | `matchPurposeRegex` in `hasAnyCriterion()` aufnehmen. Besser noch: **eine** Funktion, die beide benutzen — die Doppelung ist die Ursache, nicht der vergessene Zweig | `RecurringRuleFacts` zeigt den Satz, den die Ableitung liefert; das Set rechnet ihn nicht nach **Erledigt 2026-09-08, App-Commit `2c0c888f`.** `MATCH_CRITERIA` steht einmal; `hasAnyCriterion()` und `matchTransaction()` lesen dieselbe Liste. Die Doppelung ist weg, nicht nur der vergessene Zweig | Prüfung `entitaeten/recurring-rule.md`, 2026-09-08 |
| ~~**L-254**~~ | **Jede von Hand angelegte Regel sticht still jede importierte.** `RuleEditorForm.tsx:165` legt neue Regeln mit `priority: existingRule?.priority ?? 0` an — also **0**, während Agent und Bestand auf **100** stehen und `order by priority asc` gilt. Wer eine Regel im Formular anlegt, setzt sie damit vor alle importierten, ohne dass die Oberfläche das Feld überhaupt zeigt. Bei einem Sachverhalt mit zwei Regeln (L-245) entscheidet das, welche greift — **erledigt 2026-09-08 (App-Seite): Commit `9a3ce2db`.** `RULE_PRIORITY_DEFAULT = 100` steht einmal in der Domäne, alle drei Schreiber lesen sie. Das Prioritätsfeld bleibt im Editor unangetastet; wann es sichtbar wird, klärt **L-245** | Die Vorgabe auf denselben Wert wie beim Agenten setzen (100), oder die Reihenfolge sichtbar machen — eine Rangfolge, die niemand sieht und jeder Klick ändert, ist keine | die Regel-Formen zeigen `priority` bis dahin nicht (Rang 20, 0 % Streuung im Bestand) **Erledigt 2026-09-08, App-Commit `9a3ce2db`.** Eine Prioritäts-Konstante für alle drei Schreiber (100) | Prüfung `entitaeten/recurring-rule.md`, 2026-09-08 |
| ~~**L-258**~~ | **Die Kontoseite hat keinen Rohdaten-Reiter.** `app/(app)/clients/[clientSlug]/[year]/accounts/[accountNumber]/page.tsx` Z. 79–82 führt vier Reiter (`uebersicht`, `buchungen`, `monate`, `llm`); `rohdaten` fehlt — obwohl die generische `ui/RawDataView.tsx` seit L-31 existiert und die beiden anderen Detailseiten je einen haben (`DOC_TABS` in `source-docs/domain/tabs.ts`, `SvTab` in `sachverhalt/parts.tsx`). Der Detailseiten-Standard **D12** macht ihn zur Pflicht auf jeder Entitäts-Detailseite: immer der letzte, überall gleich benannt, für alle sichtbar, ohne Zähler und ohne Alarm. **Zu tun:** `rohdaten` als letzten Reiter mit `RawDataView` über die Kontozeile (plus Anreicherung, jahresfrei) **Erledigt 2026-09-08, App-Commit `b2a97f64`.** Die Kontoseite hat ihren Rohdaten-Reiter | `docs/detailseiten-standard.md` D12 · Prüfung `seiten/konto-detail.md`, 2026-09-08 |
| ~~**L-259**~~ | **Die Reiterleiste des Sachverhalts mischt zwei URL-Mechanismen in einer Zeile.** `SvTabsBar` (`accounting-cases/ui/sachverhalt/parts.tsx` ab Z. 341) rendert acht optisch gleiche Einträge: `uebersicht`, `saldo` und `rueckfragen` gehen über `?tab=uebersicht&view=<id>` (bzw. den Client-Callback `onView`) und sind damit **Sichten eines** Reiters; `belege`, `datev`, `plausibilitaet`, `rohdaten`, `regelwerk`, `zuordnung`, `historie` gehen über `?tab=<id>` und sind echte Reiter. Dazu kommen die Unterreiter im Detail von Rang 5. Für die Leserin ist der Unterschied unsichtbar, für den Deeplink nicht. Standard **D10**: eine Leiste, ein Mechanismus, keine zweite Ebene. **Zu tun:** die drei `view`-Einträge entweder zu echten Reitern machen oder aus der Leiste nehmen — sie sind Zonen der Übersicht (Standard §2.2) **Erledigt 2026-09-08, App-Commit `f166fc45`.** Weg 2: die drei `view`-Einträge sind Zonen der Übersicht geworden — offen (Rückfragen, fehlende Belege) → Konten → Strang mit gewähltem Ereignis; `?view=` ist weg. Damit trägt die Leiste einen Mechanismus | `docs/detailseiten-standard.md` D10 · Prüfung `seiten/sachverhalt-detail.md`, 2026-09-08 |
| ~~**L-260**~~ | **Der erste Reiter heißt auf jeder Detailseite anders.** Beleg „Beleg" (`DOC_TAB_LABEL.beleg`), Konto „Übersicht" (`accounts/[accountNumber]/page.tsx` Z. 79), Sachverhalt „Übersicht" (`SvTabsBar`). Standard **D11**: der erste Reiter heißt überall **„Übersicht"** — nicht nach der Entität und nicht nach der Ausprägung. Das ist der Nachfolger von **L-92**: dort wurde nur zwischen den Belegarten verglichen und deshalb auf „Beleg" geschlossen; über die Seiten hinweg ist auch der Entitätsname eine wechselnde Aufschrift für denselben Inhalt (die erste Ansicht des Datensatzes). **Zu tun:** `DOC_TABS[0]` von `beleg` auf `uebersicht` ziehen — Slug und Label —, `parseDocTab` leitet `beleg` um, wie es `buchung` schon tut; `availableDocTabs` und `DOC_TAB_LABEL` mit **Erledigt 2026-09-08, App-Commit `b2a97f64`.** Der erste Reiter heißt überall „Übersicht“; zwei alte Slugs leiten um | `docs/detailseiten-standard.md` D11 · L-92, 2026-09-08 |

## E · Ablösungen — welcher v3-Baustein ersetzt was

Das sind **keine Befunde**, sondern die Migrationsschritte. Sie stehen in den
Specs als Kriterien „offen (App)": hier nicht erfüllbar, weil dieses Repo das
ausgelagerte Design-System ist (`docs/backlog/README.md`).
**Der Sachverhalts-Drawer speist seinen Kopf künftig aus `CaseDetail`**
(Owner-Entscheid 2026-09-07). `CaseQuickView` trägt nur noch `facts` und
`eventCount`; Titel, Gegenpart, Betrag und Währung kommen aus dem Datensatz,
die Zuständigkeit als Schlüssel `disposition` — das Wort holt die Registry.
Wer den Drawer in der App einbaut, reicht also `CaseDetail` durch und baut
keinen zweiten, flachen Satz daneben.

**Stand der Migration (Rolle A, nachgesehen am 2026-09-07):** 141 Dateien der
App importieren aus dem Set, 208 Import-Zeilen, **68 verschiedene Namen — alle
im Barrel vorhanden** (maschinell gegen `src/ui/v3/index.ts` geprüft, kein
toter Import). Erledigte Zeilen sind durchgestrichen und nennen den Commit der
App-Seite — wer hier etwas ablöst, trägt es selbst ein; gefunden wurde das
hier durch Nachsehen, nicht durch eine Meldung.

| v3-Baustein | Ersetzt in der App | Bewusst nicht mitgenommen |
|---|---|---|
| ~~`Drawer` / `DrawerFooter` (0042)~~ | ~~`ui/components/primitives/Drawer.tsx`~~ — **erledigt (App-Seite): Commit `26a593d7`.** Die Datei ist weg, `UrlDrawer` baut auf dem Set auf | — |
| `SourceDocumentDrawer` (0052) | `BelegDrawer` und das Sachverhalts-Gegenstück in `ui/drawers/` | — |
| `AccountDrawer` (0068) | `AccountLedgerDrawerProvider` (`ui/drawers/AccountLedgerDrawer.tsx`, 365 Z.) | Spalte „Sachverhalt" (2 % gefüllt) und die Tab-Umschaltung — beides absichtlich |
| `AccountCell` (0066) | `AccountRef` (`ui/booking/`, 5 Aufrufstellen, 13 Context-Dateien) | der Context-Weg wird ein `href` über den Search-Param (L3) |
| `accountEntryColumns` / `AccountEntryList` (0067) | drei Handtabellen: die zwei `<tbody>` in `AccountLedgerDrawer.tsx` und die zwei Auszüge im Tab „Buchungen" der Kontoseite | Spalte „Sachverhalt" wandert in den `title` |
| `AccountField` (0013) | `onOpenLedger` am `JournalEntryEditor` — der reicht es künftig an die Felder durch, statt es selbst zu zeichnen | — |
| `JournalEntryCard` (0044) | `BookingProposalCompact` (`EventStack.tsx:127`) | Status, Herkunft, Konfidenz, Belegfeld 1 — stehen künftig **neben** der Karte (`StatusBadge`, `FieldList`) |
| `CaseTimeline` (0040) | `Timeline`/`TimelineItem`/`EventIcon` in `SachverhaltScreen.tsx`/`parts.tsx` | die bewusst ins Detail verschobenen Punkte |
| `RawRecord` (0051) | `RecordValue`/`RowKeyValueTable`/`CollapsibleText` in **beiden** `RohdatenTab.tsx`, `fmtRawValue` in `ui/drawers/server.tsx` | — |
| `LogBrowser` (0054) | `InvoiceLogsPanel` samt verbose-Schalter, die Stapel-Sichten — **Teilstand:** Commit `03ab5976` hat die Log-Familie an `LogList`/`LogBrowser` gegeben und damit L-33 geschlossen, aber `modules/invoices/ui/logs/InvoiceLogsPanel.tsx` steht noch (221 Z., eigener Umschalter Fachlich/Technisch/Beides, `verboseHiddenCount`). Nachgesehen am 2026-09-07 | — |
| `DataTable` (0057) | den Tabellenteil der 13 Listenseiten, zuerst `[year]/cases/page.tsx`; die zwei v2-`SelectionBar`-Stellen | — |
| `ClarificationCard` (0060) | `AnswerInput` und die Portal-Kopie | — |
| Beleg-Familie (`SourceDocumentCell`/`Row`/`Preview`/`Facts`) | `BelegSummary`, `BelegPreview`, `SourceDocFactsCard`, den Fakten-Teil von `GlanceCard` und `ContractDetail`, `ClassificationStack`, `InvoiceNumberCell`, die `FieldList` unter den zwei Spalten von `documents/[sourceDocId]` (0120: Konfidenz, Korrektur, DATEV-Ablage), die Zeilen von sechs Listen | die Positionen (0072/0114/0115 — die **Vorsteuer** gehört der Rechnung, nicht der Position), der Vertrags-Editor (0073), der View mit seinen sechs Tabs (0071) |
| `KpiTile href` (0126) | den Link im Untertitel der Kennzahl-Kachel auf der DATEV-Seite (`ponytail:`-Notiz drüben) — der Anker umschließt künftig die ganze Kachel | die Dashboard-Kachel `Stat`/`StatGrid`: andere Größe, anderer Ort, sie bleibt (Abschnitt F) |
| ~~`StatusHeader` (0077)~~ | ~~`ui/components/primitives/StatusHeader.tsx` in 36 Dateien~~ — **erledigt (App-Seite): Commit `40f8f085`.** Die Datei ist weg | `hint` (20 Stellen) und `legend` von Hand (8 Stellen → L-51); die Hover-Legende — das Set erklärt per Klick im `StatusInfoDialog` |
| ~~`Confidence` (0078)~~ | ~~`ConfidenceDot`, `ConfidenceMeter`/`ConfidenceBand`, `Confidence` (invoices) in zwölf Dateien~~ — **erledigt (App-Seite): Commit `40f8f085`.** Kein Treffer mehr auf `ConfidenceDot`, `ConfidenceMeter` oder `.confdot` | Meter (drei Balken) und Band — ein Anteil als Balken ist `Progress` |
| ~~`Wizard` (0079)~~ | ~~`ui/components/wizard/Wizard.tsx` in fünf Dateien~~ — **erledigt (App-Seite): Commit `40f8f085`.** Die Datei ist weg | die `h2`/`.sub`-Typografie im Body |
| `FilterBar` (0003) | die Formularzeile von `AccountFilterForm` | — |
| `Skeleton` (0016) | drei handgebaute Lade-Platzhalter, dazu elf `Suspense`-Fallbacks | — |
| `RadioGroup` (0017) | drei rohe `<input type="radio">` in `DatevExportWizard.tsx` und `BatchActions.tsx` | — |
| `NavList` (0031) | das Navigations-Rendering in `Sidebar.tsx` (238 Z.) | die `.sb__nav*`-Klassen bleiben Bestand |
| `CaseCell` (0095) | `ui/case/CaseCell.tsx` in `documents/page` und `StuckDocumentsTable` **und** die Kopie in `KontoauszugView` (Z. 134–172) | — |
| `CaseRow` (0096) | die Zeile in `[year]/cases/page.tsx` samt sechs Zell-Hilfskomponenten und `CaseSummaryTooltip`, dazu `CasesTab.tsx` | — |
| `CaseFacts` (0097) | die Faktenzeile aus `parts.tsx` `Hero`, die Portal-Meta, der Streifen aus `CaseOverviewBox` | die drei zeigen heute drei verschiedene Sätze |
| `CaseDrawer` (0098) | **keinen Baustein — einen Seitenwechsel**: die drei Listen mit `CaseCell` öffnen künftig den Drawer | — |
| `BankTransactionPurpose` (0099) | `PurposeDisplay.tsx` samt `purpose.css` in fünf Dateien | `KontoauszugView` bekommt sie überhaupt erst (L-59) |
| `BankTransactionCell` (0100) | `EventStack.BankTransactionBlock`, die Auszugszeile aus `Schritt4.tsx` | — |
| `BankTransactionRow` (0101) | die Zeile aus `KontoauszugView` und aus `BankTransactionAssignmentTable`, dazu vier Helfer aus `kontoauszug-presentation.tsx` | die dritte Route `configuration/bankkonten/[accountId]/transactions` gehört mit abgelöst (L-62) |
| `BankTransactionFacts` (0102) | `BankTransactionDetail.tsx` samt `KV` | — |
| `BankTransactionDrawer` (0103) | `ui/drawers/BankTransactionDrawer.tsx` | Klasse A → B: die drei Aufrufer bringen die Zeile künftig mit |
| `StateMachine` (0069) | nichts — der Baustein ist neu. **Wartet auf L-74:** sobald die Übergänge als Daten im Spiegel stehen, bekommt `StatusInfoDialog` seine vierte Stufe und zeigt Liste **und** Diagramm (Umschalter) | `FlowModal` bleibt die Kette Beleg → Sachverhalt → Buchung und ist davon nicht betroffen |
| `Drawer` + `ClarificationCard mode="read"` (0058) | `RationaleSources`, Verweis `kind='clarification'` — er führt heute auf die Sachverhaltsseite und reißt damit den Vorgang auf, in dem die Rolle gerade steckt (Buchungs-Begründung lesen) | **Kein Baustein im Set**: der Rahmen ist Markup an der Aufrufstelle (rund acht Zeilen plus Loader — `RationaleSource` trägt nur die Id). Der Ausgang „Zum Sachverhalt →" gehört in `Drawer.footer`, wie bei 0098; die Karte kennt ihn nicht und soll ihn nicht kennen |

## F · Bleibt in der App — benannte Kompositionen (Owner 2026-09-04)

Kein v3-Baustein, sondern Markup aus vorhandenen Primitives an der
Aufrufstelle. Die Liste ist das Ziel des Grep-Zählers der App: was hier steht,
wird nicht mehr ans Set gemeldet. Antwort auf `ludwig/app`
`docs/backlog/F147-luecken-für-design-agent.md` §2 und §5.

| Heute in der App | Wird in der App zu |
|---|---|
| Top-Bar-Füllung (`TopBar.tsx`) | `TopBar` (0030) mit `crumb` · `search` (`SearchInput`) · `actions` |
| `MandantBand`, `MandantSwitcher` | `Popover` + `NavList`, der Link ein `TextButton` |
| `YearSwitcher`, `RememberClientYear` | `Popover` + `NavList`; der Jahreszustand ein `StatusBadge` |
| `UserMenu` | `OverflowMenu` |
| `RoleBadge` | `StatusBadge axis="rolle"` — die Achse existiert |
| `ExperimentBadge`, `ExperimentMandantBanner` | `Badge`, `Banner` |
| `ReadinessBanner`, `EmbeddingCoverageBanner` | `StatusCallout` / `Banner` |
| `EntityStatusBadgeButton` + `FlowModal` | `StatusBadge chevron` (0049) als Auslöser, `Dialog` mit drei `StatusBadge` und Entitäts-Icons; die Server Action `fetchFlowSnapshot` bleibt drüben |
| `KIND_DOT_COLOR`, `BadgeKind` | `BadgeTone`, `DotStatus`, `Badge dot` — Farbe aus Tokens, keine Hex-Tabelle (V13) |
| `TaxKeySelect` | `Select` über `TAX_KEYS` (gespiegelt in `core/datev/tax-keys.ts`) im `JournalEntryEditor` (0015) |
| `CopyTextButton` | `ActionButton` mit Clipboard-Aktion + `useToast` |
| `ManualBookingDrawer` | `Drawer` + `JournalEntryEditor`; die Datenbeschaffung bleibt im Modul |
| Schalter (Toggle) | entfällt — 0018 verworfen |
| Kommentar-/Notizstrang (`CaseCommentForm`, `ClientAgentNotesPanel`) | wartet auf L-27 |
| Sparkline in der KPI-Kachel | wartet auf das Seitenprofil des Dashboards (Welle 2) |

## Was zuerst

Wer die App anfasst, kommt am schnellsten voran, wenn er in dieser Reihenfolge
geht — sie folgt daraus, wie viele v3-Bausteine an einem Befund hängen:

**Nicht in diesem Register, damit niemand danach sucht:** L-97 („die Pager
schreiben rohe Zahlen") war ein **Set**-Befund und ist im Set behoben —
`formatCount()` in `format.ts`, gelesen von `Pagination` (0057) und
`RecordPager` (0047), Commit `49760b1`. In der App ist dafür nichts zu tun.
Die Nummer bleibt vergeben, damit sie nicht ein zweites Mal vergeben wird.

0. **L-48/L-49** (Registry) — eine Typdefinition und ein kopierter Block;
   blockiert E1.4 der App-Migration und 0080 hier. *(erledigt 2026-09-05)*
1. **L-15** (`sort`/`dir` in `PageRequest`) — hängt an allen 13 Listenseiten und
   damit an `DataTable`; ohne das sortiert weiterhin keine Seite.
2. ~~**L-09**, **L-13/L-14**, **L-06/L-07**, **L-02**~~ — *alle erledigt am
   2026-09-06 (App-Seite).* Was hier bleibt, ist der Tausch der lokalen Kopien
   gegen den Spiegel; er gehört je Familie zu ihrer Aufgabe, nicht in einen
   Sammel-Commit.
3. **L-68/L-70** (`CaseDetail` und `ExpectationRow` nach `domain/`) — beide
   hängen an Aufgaben, die gerade gebaut werden (0097, 0025), und beide sind
   ein Umzug, keine Neuentwicklung.
4. **L-74** (Übergänge als Daten) — `StateMachine` ist gebaut und zeigt bis
   dahin für fast jede Achse nur die Reihe mit dem Hinweis.
5. Der Rest nach Aufwand; die Schema-Punkte in C brauchen je eine
   Owner-Entscheidung, keine Implementierung.
